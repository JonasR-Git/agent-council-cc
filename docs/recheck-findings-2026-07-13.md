# 60-Agent Opus Recheck — Confirmed Findings (2026-07-13)

Comprehensive re-check: 53 modules, 115 agents (55 finders + adversarial verifiers), 47 confirmed findings. Each was adversarially verified by an independent Opus agent. Still verify each against the code before fixing — a verifier can be wrong.

**Summary:** 12 P1 · 34 P2 · 1 nit

## 1. [P1] --supervise turns a hard (non-rate-limit) loop throw into a silent success: exit 0, ok:true, stranded:false, zeroed metrics, possibly stranded on the integration branch

- **File:** `plugins/council/scripts/council-companion.mjs:2327` · **Category:** correctness
- **Detail:** In the `audit fix --loop` handler, the supervised branch (council-companion.mjs:2318-2323) runs `runFixLoop` through `runSupervised`. `runSupervised` deliberately CATCHES any throw (supervisor.mjs:69-73) and converts it to `last = { err, stopReason: "pass N threw: …" }`, then classifies it non-resumable and returns `{ ...last, attempts, supervisorStop: "terminal" }`. That synthetic `out` has NO `branch`, `fixed`, `failed`, `proposed`, or `spent` fields. The handler then treats it as a normal result: line 2327 sets `out.stranded = false`; line 2328 `if (out.branch)` is falsy so the checkout-back-to-base at 2329 is SKIPPED (leaving the user on the integration branch if the throw occurred on pass ≥2, which is the common case since pass 2+ runs on the integration branch); line 2332 records fixloop metrics as all-zero; line 2333 calls `reporter.done({ ok: !out.stranded })` = ok:true; and because no throw reaches `main()`'s catch, `process.exitCode` stays 0. The comment at 2324-2325 states the explicit intent ("report if the checkout couldn't complete so the user isn't stranded silently") — this path defeats it. Net: a crashed, no-op supervised run reports as a clean success. Without --supervise the same throw propagates to main() and exits 1 (visible failure), so --supervise specifically converts a hard failure into a false success — the worst outcome for the autonomous/automated use case --supervise exists for (a CI wrapper reads exit 0 as success).
- **Failure scenario:** Run `council audit fix --loop --supervise` on a branch. During pass 2 (already on the council/audit-fix-… integration branch) runFixLoop throws a non-rate-limit error (e.g. a git failure or an unexpected engine exception). runSupervised catches it (supervisor.mjs:71), returns a synthetic terminal result with no `branch`. Handler skips the checkout (out.branch undefined) → shell is left on the integration branch; sets out.stranded=false and reporter.done({ok:true}); records fixed:0/failed:0/… metrics; process exits 0. Operator/automation sees a successful, zero-fix run while the process actually crashed and abandoned them on the integration branch.
- **Verifier:** CONFIRMED for the automation path. supervisor.mjs:69-73 catches any non-rate-limit throw from runFixLoop and returns a synthetic terminal result {err, stopReason:"pass N threw…", supervisorStop:"terminal"} (line 88) with NO branch/fixed/failed/proposed/passesRun/spent. runFixLoop normally returns `branch` (audit-fixloop.mjs:346) and leaves the tree on the integration branch (stayOnBranch:true, line 259). In the handler (council-companion.mjs:2326-2333): out.branch is undefined → the checkout-back-to-base at 2328-2331 is SKIPPED → operator stays on the integration branch while out.stranded is set false; recordAuditMetrics logs fixed/failed/proposed/passes/spent all 0; reporter.done({ok:true}). On the --json path (2334-2336) outputResult just JSON.stringifies and returns — no throw reaches main()'s catch (3621-3624), so process.exitCode stays 0. Without --supervise (line 2323) the identical throw propagates to main() → exitCode=1, so --supervise specifically converts a hard crash into an exit-0/ok:true/stranded:false/zeroed-metrics false success in exactly the autonomous/CI use case it exists for, and the audit-fix-loop handler has no stranded→exitCode=1 guard (unlike plan/build at 3533). This defeats the explicit intent of the 2324-2325 comment. One overreach in the claim: in default and --html modes renderFixLoopReport (line 2730 `out.fixed.length`) throws a TypeError on the synthetic out → exit 1 (a visible ugly crash, not silent exit 0), so the pure silent-success outcome is specific to --json — but that is precisely the automated path --supervise targets, so the core P1 stands. Not P0: the fix commits survive on the abandoned integration branch; this is a fail-open false-success plus silent stranding, not data corruption.

## 2. [P1] Anonymous `export default class extends X` builds a syntactically INVALID poison twin → per-export probe false-passes the changed-line coverage gate

- **File:** `plugins/council/scripts/lib/chartest-node-harness.mjs:160` · **Category:** correctness
- **Detail:** DEFAULT_NAMED_DECL (line 160) uses the lookahead `class\s+[A-Za-z_$]`. For an anonymous default class such as `export default class extends Base {` the token immediately after `class ` is the keyword `extends`, whose leading `e` satisfies `[A-Za-z_$]`, so the lookahead WRONGLY succeeds and buildExportPoisonedSource (line 268-271) takes the 'named declaration' branch: it strips only `export default ` and keeps `class extends Base {` — an UNNAMED class declaration, which is a hard SyntaxError (a class *declaration* requires a name; the correct handling is the else-branch, which would have produced the valid `const __councilRealDefault__ = class extends Base {}`). The same misfire hits `export default class implements Foo {` (TS). The post-hoc guard at lines 284-286 is exportSnapshot, a regex/surface check that never parses or validates syntax; hasDefault and names still match, so the invalid source is returned NON-null instead of degrading. In poisonedRunDiffers (lines 353-364) a poison run whose only fault is that the twin fails to LOAD is indistinguishable from a genuine poison-triggered failure: node exits non-zero, `poisonRun.status !== 0` → returns `true` ('differs' = dependence). So the per-export probe reports the changed export as covered even for a test that never touches it. This directly inverts the stated fail-closed invariant of exportRegionsOf/buildExportPoisonedSource ('can only ever CLAIM LESS granularity than reality, never more' / 'no probe > a probe that can lie'): a mis-scan is supposed to degrade to the whole-module probe, not manufacture a false-positive dependence. (The whole-module buildPoisonedSource is unaffected — it emits synthetic valid source and does not use DEFAULT_NAMED_DECL.)
- **Failure scenario:** Target module:
  export default class extends Base { greet() { return "hi"; } }   // greet touched by refactor
  export function other() { return 9; }
A behaviour-preserving refactor (an eligible CHARTEST lens: architecture_ssot/logical_sense/docs_maintainability/dead_code) modifies the greet() line; changedLines fall inside the default export region. The accepted char-test pins only other()'s observable and never calls the default class. VERIFY phase (chartest-wiring executesChanged = harness.executesTarget) → coverage empty under the sandbox → perExportProbe: changedExportsFor attributes the change to 'default'; buildExportPoisonedSource('default') hits the DEFAULT_NAMED_DECL misfire and returns `class extends Base {`-headed source (non-null, surface matches). poisonedRunDiffers writes this twin; the test's `import Cls from "./target"` throws SyntaxError at load; node --test exits non-zero → differs=true → perExportProbe returns true → executesChanged false-passes. Because greet() is not in the pinned observable, the observe()==baseline check in verifyCharTestAfterFix also passes, so the whole §5 gate returns pass and the refactor is auto-applied — even though it silently changed greet()'s behaviour and NO test covered the change. Fix: exclude the keyword head, e.g. `class\s+(?!extends\b|implements\b)[A-Za-z_$]`, so anonymous default classes fall to the valid `const __councilRealDefault__ = class …` else-branch.
- **Verifier:** CONFIRMED (empirically, node v22.13.1). DEFAULT_NAMED_DECL (line 160) lookahead alternative `class\s+[A-Za-z_$]` misfires on `export default class extends Base {…}`: `class ` + the `e` of `extends` satisfies it, so `test()` returns true. buildExportPoisonedSource (lines 268-271) then takes the named-declaration branch, stripping only `export default ` to yield `class extends Base { … }` — a nameless class DECLARATION, a hard SyntaxError. The correct else-branch would have produced the valid `const __councilRealDefault__ = class extends Base {…}`. Verified: buildExportPoisonedSource(src,'default') returns NON-null (not degraded); changedExportsFor(src,[1]) attributes to ['default']; importing the emitted twin fails with `SyntaxError - Unexpected token 'extends'`. The post-hoc guard (lines 284-286) is exportSnapshot, a regex parseModule surface check (audit-snapshot.mjs:34-54) that never parses syntax, so hasDefault/names still match and the invalid twin is returned. In poisonedRunDiffers (lines 361-362) a twin that merely fails to LOAD gives poisonRun.status!==0, indistinguishable from a genuine poison failure → returns true (=depends). This inverts the module's own documented fail-closed invariant (comments 121-124: 'a poisoned stand-in must RELIABLY LOAD… a load-time explosion would… be falsely accepted as depends on the target'; 193/246-251: a mis-scan must DEGRADE, never claim finer granularity). Reachable: chartest-wiring.mjs:149 wires executesChanged=harness.executesTarget; executesTarget (line 494) falls to perExportProbe when the sandbox coverage doc is empty (the default documented case, lines 487-502); chartest-gate.mjs:203-216 skips the `if(!covers)` reject and returns pass:true when executesChanged() falsely returns true and the changed line is not in the pinned observable → a behaviour-changing refactor is auto-applied with no test covering the change. Not a facade; genuinely on the §5 auto-apply path. The whole-module buildPoisonedSource is unaffected (emits synthetic valid source). Fix `class\s+(?!extends\b|implements\b)[A-Za-z_$]` is correct. P1 not P0: requires the specific (but realistic) shape of an anonymous default class co-located with another export plus an accepted char-test that does not pin the changed observable.

## 3. [P1] accept-phase poison-restore FATAL is swallowed by the consumer without reverting → poisoned target silently left on disk

- **File:** `plugins/council/scripts/lib/chartest-wiring.mjs:121` · **Category:** data-loss-facade
- **Detail:** guardPoisonRestore (chartest-wiring.mjs:78-85, called from accept's finally at line 121) enforces the module's central invariant by THROWING a plain Error when the harness could not un-poison the target (h.restoreFailure set). The header promises this is 'FATAL and thrown, never swallowed ... never a silent poisoned tree'. But it is delivered through the same channel — a thrown Error — that the only wired consumer blanket-catches. In audit-fix.mjs the accept call is wrapped `try { charAccepted = await charTestGate.accept(...) } catch (err) { charAccepted = { accepted:false, reason:'char-test accept error: '+... } }` (lines 669-672); the following `if (!charAccepted.accepted)` branch (673-678) just pushes a propose-only rejection and `continue`s. Crucially, unlike the VERIFY branch (which calls revert(snapshot) = git.resetHard before continuing, line 775), the accept-reject branch performs NO revert and NO fatalAbort. So a FATAL poison-restore failure is downgraded to an ordinary 'propose-only' line and the loop proceeds, leaving the target as the poison twin (every export replaced by a function that throws __COUNCIL_POISON__, plus the COUNCIL §5 POISON PROBE banner) uncommitted in the working tree. The wiring's own last-ditch rewrite in guardPoisonRestore can also fail (recovered=false → 'MAY STILL BE POISONED on disk'), and even that distinct 'FATAL §5 char-test' message is discarded. Net: the built, gated 'never a silent poisoned tree' mechanism is unreachable in practice — a facade of safety. Fix belongs at the plumbing seam: the wiring should surface poison-restore failure as a structured, un-ignorable signal (e.g. a typed error / {fatal:true}) that the consumer is contractually required to route to fatalAbort or a git reset, and audit-fix's accept-reject branch must revert/abort rather than continue when the tree may be dirty.
- **Failure scenario:** `audit fix --chartest` on a repo with a refactor-class (architecture_ssot/dead_code/etc.) finding. accept generates a valid char-test; the whole-module poison probe writes the poisoned twin over the target, then restoreTarget's 3 write+read-back attempts all fail (plausible on this win32 environment: transient AV scan / file lock / EPERM), and guardPoisonRestore's single last-ditch writeFile also fails. restoreFailure is set, acceptCharTest returns accept:false, guardPoisonRestore throws 'FATAL §5 char-test: ... MAY STILL BE POISONED'. audit-fix's catch turns it into charAccepted={accepted:false}; the loop records propose-only and continues with NO revert. The run finishes (or moves to the next finding) leaving the target file as the non-functional poison twin in the working tree, which the operator can unknowingly commit — exactly the 'silent poisoned tree' the module claims to make impossible.
- **Verifier:** CONFIRMED real. The accept-phase poison-restore FATAL is swallowed and the poisoned target is left on disk with no revert.

Chain verified against the code:
- chartest-node-harness.mjs:356 writes the poisoned twin over the REAL target (writeFile(absTarget, poisonedSource)); restoreTarget (333-347) throws FATAL after 3 failed write+read-back attempts, leaving the poison on disk and setting restoreFailure.
- chartest-wiring.mjs:118-122 accept's finally calls guardPoisonRestore, which (line 85) throws a PLAIN Error ("FATAL §5 char-test: ... MAY STILL BE POISONED on disk"). A throw in finally overrides the try result, so charTestGate.accept() rejects.
- audit-fix.mjs:668-672 wraps the accept call in its OWN try/catch that blanket-converts ANY error (incl. the FATAL) into charAccepted={accepted:false, reason:'char-test accept error: ...'}. withLimitRetry never rethrows differently (default is fn=>fn(); retry path only retries rate-limit errors).
- audit-fix.mjs:673-678 the !accepted branch pushes a "propose-only" rejection, logs, and `continue`s — skipping the entire main try block (681-877) where every revert(snapshot)/fatalAbort lives. revert = git.resetHard (603-606) is never called.

Asymmetry confirmed: the VERIFY branch catches its FATAL (771-773) and then DOES revert(snapshot) at 775; the accept branch does not. The design comment at 600-602 states the flawed assumption explicitly ("char-test not accepted ... never call [revert] — they are 'proposed'"), which is only safe for ordinary rejections that leave a clean tree; the poison-restore-FATAL subcase (dirty tree) is laundered into the same never-revert path.

Concrete failure: `audit fix --chartest` on a refactor-class finding; poison probe overwrites the target; restoreTarget's 3 writes + guardPoisonRestore's last-ditch write all fail (plausible on win32: transient AV/file lock/EPERM; deterministic under read-only dir / disk full). Result: the non-functional poison twin (every export throws __COUNCIL_POISON__ + the COUNCIL §5 POISON PROBE banner) is left uncommitted in the working tree, the run continues/completes, and the operator can unknowingly commit it — exactly the "silent poisoned tree" the module's guardPoisonRestore was built to make impossible. Built + gated but its protective effect is unreachable at the only consumer = the anti-facade class. Fix: surface poison-restore failure as a typed/{fatal:true} signal the consumer must route to fatalAbort or git reset, and make audit-fix's accept-reject branch revert/abort rather than continue when the tree may be dirty.

## 4. [P1] A gate throw between applyTransform and commit leaves the transform applied-but-unreverted

- **File:** `plugins/council/scripts/lib/audit-multifix.mjs:183` · **Category:** data-integrity
- **Detail:** In runOneTransform only two throws are caught by a revert: applyTransform (lines 142-146) and commitFiles (lines 196-200). The whole gate region in between (lines 149-190: deps.isDeterministic, deps.git.changedFiles, deps.readFiles, deps.acceptCharTest, deps.diffLines, deps.runOracle, deps.runTests) is NOT wrapped. If any of these throws/rejects (as opposed to returning a falsy value), the exception unwinds out of runOneTransform — there is no surrounding try after line 146 — into runMultiFix's catch at line 224, which sets res={outcome:"reject"} and pushes to `rejected`. No revert() runs, so the fully-applied N-file codemod stays on disk, uncommitted and un-rolled-back. This violates the module invariant 'Each is atomic + independently reverted' (line 206) and directly contradicts the author's comment at lines 192-194 that a throw must revert 'the same as every other gate' — the other gates do not revert on throw. The next transform then aborts on the clean-tree check (line 128) if deps.git.isClean exists (leaving the dirty tree), or, if isClean is absent, builds on top of the poisoned tree.
- **Failure scenario:** transforms=[T] with a valid deterministic-codemod. deps.applyTransform(T) writes the multi-file edit successfully. deps.runTests() rejects because the test command binary is missing (child_process spawn ENOENT) — it throws instead of returning {ok:false}. Control unwinds past runOneTransform (no try/catch covers line 183) to runMultiFix line 224, which records {outcome:'reject'}. The applied edits to all planned files remain on the working tree, uncommitted and never reverted; runMultiFix returns ok:true with a dirty, ungated multi-file change left behind — the atomic apply-or-revert guarantee is broken.
- **Verifier:** CONFIRMED. In runOneTransform (plugins/council/scripts/lib/audit-multifix.mjs) only two regions are try/catch-guarded: applyTransform (lines 142-146) and commitFiles (196-200). The gate region between them (lines 149-190: deps.isDeterministic, deps.git.changedFiles, deps.readFiles, deps.acceptCharTest, deps.diffLines/coverageOfLines, deps.runOracle, deps.runTests at line 183) has NO surrounding try/catch — each gate is handled only for a falsy RETURN (e.g. line 184), never for a throw. Concrete trace: applyTransform writes the N-file edit to disk; deps.runTests() at line 183 rejects (e.g. child_process spawn ENOENT); the exception unwinds out of runOneTransform (no covering try) into runMultiFix's catch at line 224, which sets res={outcome:'reject'} and pushes to `rejected` WITHOUT calling revert(). No resetHard runs, the fully-applied multi-file codemod stays dirty on the tree, git.isClean()===false, and runMultiFix returns ok:true (aborted null). This violates the module invariant 'Each is atomic + independently reverted' (line 205) and contradicts the author's own comment at 192-194 ('a throw here must revert the same as every other gate') — the other gates do NOT revert on throw. The author added the commit try/catch plus a dedicated test (tests/audit-multifix.test.mjs line 152) but left the identical gap on the gate region; the red-suite test (line 101) only exercises runTests returning {ok:false}, never a throw, so the throw path is untested and unhandled. Mitigating context: runMultiFix is referenced only by its test file, i.e. not yet wired into the production loop (matches the module comment at lines 11-12), so current blast radius is zero — but this is the safety-critical rollback harness whose sole purpose is the atomicity guarantee it fails to provide, so it is a real latent P1 data-integrity/atomicity defect.

## 5. [P1] applyPeerVotes leaks a peer 'agree' vote to any other merged finding sharing the same normalized title (different file) -> fabricated consensus

- **File:** `plugins/council/scripts/lib/findings.mjs:530` · **Category:** correctness
- **Detail:** In applyPeerVotes (lines 527-538) the per-item `related` filter matches a vote to an item if EITHER `v.targetId` is in `item.ids` OR `normalizedTitle(v.title) === normalizedTitle(item.title)`. The title clause is applied unconditionally, even when the vote also carries a targetId that unambiguously points at a DIFFERENT finding. Because mergeFindings keys buckets on file+title, two findings with the SAME title in DIFFERENT files remain two distinct items in merged.all (fuzzyMatch requires same file, so they never merge). A peer 'agree' vote about one of them is therefore credited to BOTH via the title match, and the second item gets a foreign voter appended to voteAgents, flipping agreement to >=2 and promoting a genuine single-agent finding to `consensus`. This is exactly the fabricated-consensus outcome the module claims to prevent: consensus items skip the adversarial refutation pass, are prioritized in the report, and carry more weight downstream (§6 gating / prioritization). The title fallback should only fire when the vote lacks a matching targetId.
- **Failure scenario:** merged.all = [item1{agents:[codex], ids:[codex-1], file:src/a.js, line:10, title:'Missing null check'}, item2{agents:[claude], ids:[claude-1], file:src/b.js, line:20, title:'Missing null check'}]. grok critiques codex and emits vote {targetId:'codex-1', title:'Missing null check', vote:'agree'}. For item1 the targetId matches (correct). For item2 the targetId does NOT match (ids=[claude-1]) but the title clause matches, so grok is added to item2.voteAgents; item2 agreement becomes claude+grok=2 -> consensus:true. Result: claude's independent src/b.js finding is reported as a two-seat consensus that grok never actually agreed with, and it is exempted from refutation.
- **Verifier:** CONFIRMED real P1. In applyPeerVotes (findings.mjs:527-538) the per-item `related` filter is a plain OR: L529 matches on targetId-in-item.ids, L530 matches on normalized-title equality. The title clause (L530) has no guard suppressing it when the vote's targetId is present and resolves to a DIFFERENT item, and there is no vote.about-vs-item.agents guard either. All preconditions hold in real usage: (1) two same-title findings in different files stay distinct — findingKey (L316) keys on file::title and fuzzyMatch (L357-369) requires same file, so item1{src/a.js} and item2{src/b.js} never merge and both survive in merged.all; (2) votes carry BOTH targetId and title by DEFAULT, not as an edge case — the R2 prompt template prompts/r2-peer-critique.md L37-44 explicitly instructs every vote to emit "targetId":"id-from-their-finding" and "title":"their title", and the vote-builder L515-524 preserves both; (3) generic titles like 'Missing null check' realistically recur across files/seats. Concrete failure: codex raises {id:codex-1, src/a.js, 'Missing null check'}; claude independently raises {id:claude-1, src/b.js, 'Missing null check'}; grok critiques codex and emits {targetId:'codex-1', title:'Missing null check', vote:'agree'}. For item2 (ids=[claude-1]) L529 is false but L530 matches on title, so grok is pushed to item2.voteAgents (L534-537), agreement=1+1=2, consensus=true (L539-545). claude's single-agent src/b.js finding is now a fabricated two-seat consensus grok never voted on. Downstream harm confirmed: applyPeerVotes (deliberate.mjs:457) runs before verifyFindings (deliberate.mjs:506); shouldVerify (verify.mjs:65) returns !finding.consensus so item2 skips the adversarial refutation pass entirely; partitionByRefutation (verify.mjs:171) permanently protects consensus from demotion; sortMergedItems (findings.mjs:400) prioritizes it. This is exactly the title-collision fabricated-consensus the module's knownLine/lineClose machinery (L340-355) defends against on the MERGE path, but the VOTE path leaves the identical hole open. Fix: fire the title fallback only when the vote lacks a targetId that resolves against any merged item (or gate by vote.about). Adversarial refutation attempts failed — the title clause is the only matcher for item2, items provably don't merge, and no aboutAgent guard exists.

## 6. [P1] runCodexStructured's standalone-CLI fallback is unreachable on the `council audit` path — codex-CLI-only users get a silently dead finder

- **File:** `plugins/council/scripts/lib/agents.mjs:346` · **Category:** facade
- **Detail:** runCodexStructured (agents.mjs:344-346) falls back to the standalone `codex exec` CLI (runCodexCli) when the companion is absent but `backends.codex.cli.available` is true. Its docstring and the pinning test (tests/agents-codex-cli.test.mjs:6-12) state this exists to purge the 'silently dead sixth eye' — a user with the `codex` CLI but without the OpenAI-Codex Claude plugin. But the audit finder dispatch does NOT go through this function unconditionally: reviewUnit (audit-review.mjs:234) and globalReduce (audit-review.mjs:283) iterate ONLY `activeSeatNames(backends, options)`, which filters via `seatActive` (seats.mjs:31): `seatActive('codex') = !skipCodex && Boolean(backends.codex.companionAvailable)` — it never consults `cli.available`. So on `council audit` a codex-CLI-only user is filtered out BEFORE runCodexStructured is ever called; the CLI fallback branch (agents.mjs:346) is dead code on that path. By contrast the `council review`/deliberate path dispatches the runner directly (deliberate.mjs:291-317, letting runCodexStructured self-select CLI/companion/skip), so the fallback IS reached there — proving the wiring is inconsistent, not the function. The stale comment at audit-patch-reviewer.mjs:199-200 ('runCodexStructured ... HARD-requires the companion — hence companionAvailable, never cli.available') encodes the now-false premise that keeps the gate companion-only. The unit test only exercises the function in isolation; nothing tests that the audit path DISPATCHES codex when only the CLI is present, so the facade (built + gated + unreachable) slipped through — exactly the recurring failure class this codebase has flagged.
- **Failure scenario:** User has `codex` on PATH but NOT the OpenAI-Codex Claude plugin. probeBackends -> codex.companionAvailable=false, codex.cli.available=true. Run `council audit`: activeSeatNames -> seatActive('codex') -> false, so reviewUnit and globalReduce dispatch only grok/claude. Codex never reviews, never contributes to consensus on the audit path (runCodexStructured's CLI branch is never invoked) — the six-eyes invariant is silently reduced to four eyes for this config, and the audit report marks codex `false` despite a working CLI. The same user's `council review` DOES run codex via the CLI fallback, confirming the drop is a wiring gap, not a limitation.
- **Verifier:** CONFIRMED. seatActive('codex') (seats.mjs:31) gates on backends.codex.companionAvailable ONLY, never cli.available. Every audit finder dispatch filters through activeSeatNames before invoking any runner: reviewUnit (audit-review.mjs:234), globalReduce (audit-review.mjs:283), and the current grouped path runGroupedReview via activeModels (audit-grouped-review.mjs:38,59). probeBackends (discover.mjs:150-154) sets companionAvailable and cli.available independently, so a user with `codex` on PATH but without the OpenAI-Codex Claude plugin gets companionAvailable=false, cli.available=true. That user is filtered OUT of activeSeatNames, so runCodexStructured — and its standalone-CLI fallback runCodexCli at agents.mjs:346 — is never reached on the audit path: dead code on that path. By contrast deliberate.mjs:291 iterates allSeatNames() and applies seatActive only to OpenRouter seats (:303); built-in codex is dispatched unconditionally via runCodexStructured(...,\"r1\"), so the CLI fallback IS reached on the `council review` path. Same config → codex reviews on `council review`, silently dropped on `council audit`, six-eyes degrades to four-eyes, and the report marks codex false despite a working CLI. The fallback was built explicitly for this context: the pinning test agents-codex-cli.test.mjs:19 calls runCodexStructured with label \"audit\", but only exercises the function in isolation — nothing tests that the audit DISPATCH includes codex when only the CLI is present. Built + gated + unreachable = the recurring facade class. The stale comment audit-patch-reviewer.mjs:199-200 ('HARD-requires the companion — hence companionAvailable, never cli.available') encodes the now-false premise; because seatActive is shared between the (correctly fail-closed) §6 gate and the finder dispatch, the finder path wrongly inherits the companion-only restriction. Could not construct any refutation: no code path re-flips companionAvailable when the CLI is present, and the audit command uses probeBackends directly. Not P0 because the audit still completes with the remaining seats and the §6 auto-apply gate stays fail-closed (safe); it is a silent coverage/invariant reduction for the CLI-only-no-plugin config.

## 7. [P1] On the default /council:solve path claude never critiques and its plan is scored by a LARGER pool — the six-eyes/symmetric-ranking refactor is a facade that only holds when claude.cli is (needlessly) probed available

- **File:** `plugins/council/scripts/lib/solve.mjs:332` · **Category:** facade
- **Detail:** The whole point of the refactor (docstring lines 228-242, comment lines 259-262, tests A10) is that even when claude hands in its plan as a FILE it "still CRITIQUES like any other seat, so the critique pools stay symmetric." That is FALSE on the real, documented invocation.

Root cause chain:
- `seats = activeSeatNames(backends, options)` is computed once (solve.mjs:257) and the critique loop draws critics verbatim from it (solve.mjs:332 `for (const critic of seats)`). There is no code path that injects claude into the critic pool when it submits via file.
- `seatActive("claude")` (seats.mjs:33) returns `!skipClaude && Boolean(backends.claude.cli.available)`. Claude is in `seats` ONLY if the claude CLI was probed available.
- `executeCouncilReview` (council-companion.mjs:599-600) probes the claude CLI ONLY when `normalizeClaudeBackend(...) === "spawn"`. The default policy is `claude_backend: "session"` (policy.mjs:30), so `needsClaudeProbe=false` → `probeBackends(..., {probeClaude:false})` → `claudeCli = {available:false}` (discover.mjs:142-145).
- The documented solve command (commands/solve.md:31) always passes `--claude-plan-wait`, so in runSolve `claudePlanFromFile=true` (solve.mjs:263-264): claude is skipped in R1 (solve.mjs:268) and its plan is loaded from the file into `plans` (solve.mjs:312-315).

Net effect on the default run: `seats = ["codex","grok"]` (claude excluded — CLI "not probed"). (1) Claude — a full proposing seat — critiques ZERO peer plans (six-eyes broken: a finder that never runs the critique lens). (2) In the critique loop each of codex's/grok's plans is scored by seats\{self} = 1 critic, but claude's plan (author not in `seats`) is scored by BOTH codex and grok = 2 critics. rankPlans (solve.mjs:115-133) then averages claude's overall over 2 votes vs codex/grok over 1 vote — averaging over DIFFERENT-sized pools, which is exactly the "rankPlans vergleicht Mittelwerte über UNTERSCHIEDLICHE Kritiker-Pools" bug the refactor claims to have killed.

Why the tests miss it (the facade): solve.test.mjs `seatBackends()` hardcodes `claude:{cli:{available:true}}`, which the real session-backend solve path (probeClaude:false) never produces. So A10 'pools are SYMMETRIC' and the plan-file test both go green while production is asymmetric and claude-mute. Fix: derive the critic/proposer pool so that any seat that actually submitted a plan (incl. claude-from-file) is in the pool, or force `probeClaude:true`/require claude.cli.available whenever a claude plan file is supplied.
- **Failure scenario:** Run the documented command `council-companion.mjs solve --problem-file p.md --claude-plan-wait claude-plan.json` with default policy (claude_backend: session). executeCouncilReview sets needsClaudeProbe=false → backends.claude.cli.available=false → activeSeatNames returns ["codex","grok"]. claudePlanFromFile=true, so claude's plan is loaded from the file and pushed into `plans`. In the critique loop critics∈{codex,grok}: codex's plan gets 1 vote (grok), grok's plan gets 1 vote (codex), claude's plan gets 2 votes (codex+grok). Outcome: claude critiques nothing (six-eyes violated) and run.ranking has claude.votes=2 vs codex/grok votes=1, so the average-overall ranking is computed over incomparable pools — the very defect the six-eyes refactor was written to eliminate.
- **Verifier:** CONFIRMED by reading the full chain. On the default, documented /council:solve path the six-eyes/symmetric-pool refactor is a facade. Chain: solve.md:31 invokes `solve --claude-plan-wait ...` WITHOUT `--claude-backend spawn`; executeCouncilReview (council-companion.mjs:599-600) computes needsClaudeProbe = normalizeClaudeBackend(policy.claude_backend) === "spawn", and policy.mjs:30 defaults claude_backend to "session" (policy.mjs:297-298 keeps non-"spawn" as "session"), so needsClaudeProbe=false -> probeBackends({probeClaude:false}) -> discover.mjs:142-145 sets backends.claude.cli = {available:false,"not probed"}. In runSolve, seats = activeSeatNames(...) (solve.mjs:257): seatActive("claude") (seats.mjs:33) = !skipClaude && Boolean(false) = false, so seats = ["codex","grok"]. Meanwhile claudePlanFromFile=true (solve.mjs:263-264) loads claude's plan from the file into `plans` (solve.mjs:312-315). Nothing ever injects "claude" into `seats`. Net effect: (1) the critique loop `for (const critic of seats)` (solve.mjs:332) runs only codex+grok, so claude — a full proposing seat — critiques ZERO peer plans, violating the six-eyes invariant. (2) codex's plan gets 1 vote (grok), grok's gets 1 vote (codex), but claude's plan (author not in seats) gets 2 votes (codex+grok); rankPlans (solve.mjs:115-133) then averages claude's overall over 2 votes vs codex/grok over 1 — averaging over different-sized pools, exactly the bug the docstring (solve.mjs:236-238) and comment (solve.mjs:259-262) claim to have killed. Both foreground (:1025) and --background (handleWorker :1047) go through the same probing, so the recommended background command is affected too. Facade proof: tests/solve.test.mjs seatBackends() (:121-129) hardcodes claude.cli.available=true; the plan-file test (:229-244) and A10 SYMMETRIC test (:186-197) therefore assert claude critiques and votes=[2]/[3], but never exercise the claude.cli.available=false + claudePlanFromFile=true state the documented session-backend path always produces, so they stay green while production is claude-mute and asymmetric. Real P1 correctness/facade defect.

## 8. [P1] Entrypoint mandatory-coverage signal is dead-wired — gate can report complete without ever reviewing the CLI entrypoint

- **File:** `plugins/council/scripts/lib/audit-run.mjs:17` · **Category:** facade
- **Detail:** codebase-model.buildCodebaseModel exposes entrypoints ONLY at model.graph.entrypoints (return object, codebase-model.mjs:266) and sets NO isEntrypoint field on its per-file facts (facts object built at codebase-model.mjs:210-227). The audit consumer enrich() reads model.entrypoints (undefined — should be model.graph.entrypoints) and fact.isEntrypoint (never set), so isEntrypoint is ALWAYS false. mandatorySet's `else if (f.isEntrypoint) reason = "entrypoint"` branch (audit-targets.mjs:61) therefore never fires — the entire entrypoint mandatory reason is unreachable. That council-companion.mjs:2220 reads model.graph?.entrypoints (correct) while audit-run.mjs:17 reads model?.entrypoints proves graph.entrypoints is the real contract and audit-run is the outlier. Production hits this dead path: council-companion.mjs:1984 calls runAudit with no deps, so inv comes from inventoryFiles and enrich() runs. Anti-facade violation: mandatorySet's entrypoint branch is built + tested (audit-targets.test.mjs:48) but never reached in the audit pipeline.
- **Failure scenario:** Audit any repo whose only high-risk-but-uncovered file is the entrypoint — e.g. plugins/council/scripts/council-companion.mjs, a 2000+-line CLI matched by no other mandatory rule (not security/manifest/infra) and with fan-in 0 (entrypoints aren't imported, so not high-fan-in). Because isEntrypoint is always false it is excluded from mand.ids. If every OTHER mandatory JS file is reviewed, summarizeCoverage returns mandatory.complete === true and gateStatus returns "pass" — the audit reports complete/pass having never reviewed the primary entrypoint.
- **Verifier:** Confirmed dead-wire/facade defect. Producer buildCodebaseModel exposes entrypoints ONLY at model.graph.entrypoints (codebase-model.mjs:266) and sets no isEntrypoint on per-file facts (codebase-model.mjs:210-227). Consumer enrich() (audit-run.mjs:17,23) reads the wrong locations: model?.entrypoints (undefined in production — real field is model.graph.entrypoints) and fact?.isEntrypoint (never set), so isEntrypoint is ALWAYS false. Thus mandatorySet's `else if (f.isEntrypoint) reason="entrypoint"` branch (audit-targets.mjs:61) is unreachable in the audit pipeline. Production reach is real: council-companion.mjs:1984 calls runAudit with model=buildCodebaseModel(...) and no deps, so inventoryFiles + enrich run. The correct contract is proven by council-companion.mjs:2220 reading model.graph?.entrypoints; audit-run.mjs:17 is the lone outlier. Anti-facade smoking gun: the entrypoint branch is tested (tests/audit-targets.test.mjs:60) but tests/audit-run.test.mjs:8 fabricates a model as {files, entrypoints:[]} (top-level, matching the buggy consumer, not the real producer's graph.entrypoints) — which is why the mismatch went uncaught. Failure scenario: an entrypoint matched by no other mandatory rule (e.g. the CLI council-companion.mjs — 2000+ LOC, fan-in 0 since entrypoints aren't imported, not security/manifest/infra) is excluded from mand.ids; its coverage unit gets mandatory:false (audit-run.mjs:53) and it is absent from mandatoryUnreviewed (line 57 requires mandIds.has). If every other mandatory JS file is reviewed, summarizeCoverage returns mandatory.complete===true (audit-coverage.mjs:45) and gateStatus returns "pass" (lines 55-58) — the gate reports complete/pass having never reviewed the primary entrypoint. No compensating guard exists and no other code sets model.entrypoints or facts.isEntrypoint. P1: defeats the documented mandatory-coverage invariant for entrypoints (built+gated+unreachable), the exact fail-closed-hiding facade class this repo flags.

## 9. [P1] TS/JSX files are classified 'js' but never enumerated/parsed — audit gate is permanently indeterminate on TypeScript/JSX repos, and state=parsed is a mislabel

- **File:** `plugins/council/scripts/lib/codebase-model.mjs:15` · **Category:** coverage-gap
- **Detail:** enumerateFiles builds model.files using SOURCE_RE = /\.(mjs|cjs|js)$/ (codebase-model.mjs:15), which excludes .jsx/.ts/.tsx. But FILE_CLASS (codebase-model.mjs:77-85) classifies .jsx/.ts/.tsx as "js", so inventoryFiles tags them class "js". In runAudit a non-reviewed js-class file is given state "parsed" (audit-run.mjs:48-53), yet such a file is never in model.files, so selectUnits (audit-review.mjs:66-74, which reads model.files) can never select it and it can never be reviewed. The FILE_CLASS comment 'The import-graph + complexity detectors parse only the js class today' is inaccurate: the js class is strictly broader than SOURCE_RE, so part of the js class is mapped-as-js but never actually parsed, and 'parsed' is reported for files that were never parsed.
- **Failure scenario:** Audit a TypeScript project containing e.g. src/auth/session.ts (security-sensitive path -> mandatory; fileClass 'js'). It can never enter model.files, never be selected by selectUnits, never reviewed -> it stays mandatory-and-not-done -> summarizeCoverage.mandatory.complete stays false -> gateStatus returns 'indeterminate' on every pass, including after unbounded /loop passes. The audit can never reach pass or fail on a TS/JSX codebase. Fail-closed (no false pass), but the gate is unusable for such repos and mislabels these files as 'parsed'.
- **Verifier:** Confirmed by reading the code end-to-end. enumerateFiles/walk gate model.files on SOURCE_RE=/\.(mjs|cjs|js)$/ (codebase-model.mjs:15,44,69), excluding .jsx/.ts/.tsx. But fileClassOf classifies those as "js" via /\.(mjs|cjs|jsx?|tsx?)$/ (codebase-model.mjs:78), so inventoryFiles tags e.g. src/auth/session.ts as fileClass "js". mandatorySet marks it mandatory (SECURITY_PATH matches /auth, audit-targets.mjs:9,56). BOTH review engines derive their file list from selectUnits(model,…) which reads model.files — the per-file runAuditReview (audit-review.mjs:61-74) and the newer M7 runGroupedReview (audit-grouped-review.mjs:61). Since the .ts file is never in model.files, neither engine can select or review it, so it can never enter runAudit's `reviewed` set. runAudit therefore gives it state "parsed" (audit-run.mjs:51) and lists it in mandatoryUnreviewed (line 57). summarizeCoverage does not count "parsed" as done (audit-coverage.mjs:35), so mandatoryDone < mandatoryTotal → mandatory.complete=false (line 45) → gateStatus returns "indeterminate" every pass (line 57), including after unbounded /loop offset passes (offset only advances within model.files). Refutations fail: TS is explicitly in scope (FILE_CLASS deliberately maps tsx? to js; code-other covers other langs), the grouped path shares the same model.files source, and no path can inject a non-model.files id into `reviewed`. Trigger is common: any .ts/.tsx/.jsx on a path matching auth|session|login|token|jwt|crypto|oauth|acl|permission|sanitiz|escape (ubiquitous in real TS apps). Two real effects: (1) the audit gate — the tool's primary verdict — is permanently indeterminate (never pass/fail) for essentially every real TypeScript/JSX repo; (2) such files are reported state "parsed" in coverage.byState though the parser never touched them (SOURCE_RE ⊊ "js"), a mapped-as-parseable-but-never-reached facade that also contradicts the codebase-model.mjs:74-76 comment. Fail-closed (no false pass/data loss/security bypass), so not P0; but it disables the core gate on a major language ecosystem and mislabels coverage → P1, not a P2 nit.

## 10. [P1] discover's codex.cli.available probe is ignored by the seat gate — CLI-only codex is silently dropped; runCodexCli fallback is unreachable

- **File:** `plugins/council/scripts/lib/seats.mjs:31` · **Category:** facade-silent-seat-drop
- **Detail:** probeBackends() correctly computes BOTH codex.companionAvailable (companion file exists) and codex.cli.available (real `codex --version` probe). The council-companion preflight AND doctor honor the CLI probe: reachability is `companionAvailable || cli.available` (council-companion.mjs:263) and doctor prints the CLI detail when the companion is absent (council-companion.mjs:306). But the seat registry ignores it: seatActive('codex') returns `Boolean(backends?.codex?.companionAvailable)` with NO `|| cli.available` (seats.mjs:31). Every finder/verify/deliberate/solve/benchmark/§6 path enumerates seats through activeSeatNames→seatActive (audit-review.mjs:234/283, verify.mjs:36, deliberate.mjs, patchReviewerReady audit-patch-reviewer.mjs:204). So for a codex-CLI-only user, codex is never placed in the active set, runCodexStructured is never invoked for it, and the runCodexCli fallback added inside runCodexStructured (agents.mjs:346) — whose own comment (agents.mjs:313-322) says it exists precisely to prevent 'a SILENTLY DEAD sixth eye' — is dead code on all these surfaces. The stale comment at audit-patch-reviewer.mjs:199-200 ('the codex SEAT votes via runCodexStructured, which HARD-requires the companion') documents the pre-fallback behavior and no longer matches the code. Fix: seatActive('codex') must be `!skipCodex && (companionAvailable || cli.available)`.
- **Failure scenario:** User has the standalone `codex` CLI on PATH but has NOT installed the OpenAI-Codex Claude plugin (no codex-companion.mjs). probeBackends → codex.companionAvailable=false, codex.cli.available=true. `council doctor`/preflight report codex AVAILABLE. User runs `council audit review`: activeSeatNames→seatActive('codex')=false → codex is dropped from finders, is not an eligible verifier (verifierAvailability=false), and patchReviewerReady marks codex not-ready so §6 auto-apply can never reach unanimity (permanently propose-only). Result: the review silently runs with 5 eyes instead of 6 while the doctor claimed codex was live — the exact silently-dropped-model / facade class the runCodexCli fallback was written to eliminate.
- **Verifier:** CONFIRMED facade defect on the `council audit review` surface. discover.mjs:137,152-153 probes codex.cli.available independently of companionAvailable; council-companion.mjs:218,305-306 reports codex REACHABLE/READY when only the CLI exists (codexReachable includes `|| cli.available`), printing `reviewer [ok]`. But seats.mjs:31 gates the codex seat on companionAvailable ONLY (no `|| cli.available`). All audit surfaces enumerate seats through activeSeatNames/seatActive: audit-review.mjs:234 (reviewUnit) and :283 (globalReduce) → codex dropped from the finder set (5 eyes, not 6); verify.mjs:36 verifierAvailability → codex ineligible as refuter; audit-patch-reviewer.mjs:204 patchReviewerReady over requiredPatchSeats (always includes codex) → ready:false → §6 sensitive-class auto-apply permanently propose-only. Because seatActive gates codex out BEFORE its runner is reached, the runCodexCli fallback (agents.mjs:346), whose own comment (313-321) says it exists to prevent a 'SILENTLY DEAD sixth eye', is dead code on these surfaces; the stale comment at audit-patch-reviewer.mjs:199-200 documents the pre-fallback behavior. Concrete failure: user with the standalone `codex` CLI on PATH but no OpenAI-Codex Claude plugin → companionAvailable=false, cli.available=true → `council doctor` prints codex live/READY, yet `council audit review` runs with 5 eyes and can never reach §6 unanimity. Exactly the silently-dropped-model / built+gated+unreachable class the fallback was written to eliminate. One overreach in the claim: deliberate.mjs (the `council review` path) iterates allSeatNames and dispatches runCodexStructured directly (deliberate.mjs:277,291), so it DOES reach the CLI fallback — but the finding's alleged-failure scenario is scoped to `council audit review`, where the defect is real. Fix: seatActive('codex') = !skipCodex && (companionAvailable || cli.available).

## 11. [P1] Reset-aware retry-after wait is unreachable in production and capped below real reset times, causing early-wake thrash

- **File:** `plugins/council/scripts/lib/supervisor.mjs:96` · **Category:** correctness
- **Detail:** The docstring (supervisor.mjs:5-9, 33-36) promises the supervisor WAITS 'until the limit resets, honoring a retry-after hint (a real reset time)'. On the wired path this is dead code. (1) resetAwareWaitMs(last?.err ?? stop, …) at supervisor.mjs:96 receives a STRING because runFixLoop (audit-fixloop.mjs:346) and runEndless (audit-endless.mjs:162) return only a `stopReason` string with NO `err` and NO `retryAfter`/`retry_after` field; the supervisor's own catch (line 72) only attaches `err` when runPass itself throws, but both loops swallow their review/fix errors and resolve normally. retryAfterFrom (audit-retry.mjs:54) requires an object and returns null for a string, so the retry-after branch of backoffMs is never taken in production. (2) Even if a hint reached it, backoffMs clamps it to maxMs, whose default is 900_000 (15 min) — and both call sites pass no maxMs. The module's own header (line 4) cites 'a multi-hour reset' as the motivating case, yet the max possible wait is 15 min. So the supervisor always falls back to blind exponential backoff (30s→15min) and wakes far too early on a real Anthropic usage-window reset. The retry-after path is exercised only by unit tests feeding {retryAfter:N} objects directly (supervisor.test.mjs:33-40), never by the real loop.
- **Failure scenario:** Anthropic returns a 1-hour usage-window reset. runFixLoop/runEndless return {stopReason:'review error on pass N: rate_limit…'} with no err/retryAfter. resetAwareWaitMs gets the string → retryAfterFrom returns null → backoffMs uses exponential backoff capped at 15 min. The supervisor waits at most 15 min, resumes into a backend still ~45 min from reset, immediately re-fails, and repeats — never once actually waiting until the limit resets, defeating the module's entire reason for existing.
- **Verifier:** CONFIRMED. The supervisor's reset-aware retry-after wait is unreachable in production and its fallback is capped below real reset times. Mechanism verified end-to-end: (1) runFixLoop (audit-fixloop.mjs:346) and runEndless (audit-endless.mjs:162) return only a STRING stopReason with no err/retryAfter. Rate-limit errors are caught and stringified into stopReason at audit-fixloop.mjs:226-229 and audit-endless.mjs:132-136 (withLimitRetry/retryOnRateLimit rethrows the real error object on exhaustion, but the loop's catch destroys it — the retryAfter field never survives). (2) Both runPass closures (council-companion.mjs:2320, :2565) return the raw loop result unmodified and pass only { onWait } — no maxMs/baseMs/factor. (3) supervisor.mjs:96 does resetAwareWaitMs(last?.err ?? stop, …); last.err is always undefined in production (set only at supervisor.mjs:72, the throw path the loops never reach), so it passes the stopReason string. retryAfterFrom (audit-retry.mjs:53-54) returns null for any non-object, so the retry-after branch of backoffMs (audit-retry.mjs:48) is dead on the wired path — a facade. (4) backoffMs default maxMs=900_000 (audit-retry.mjs:46) and call sites pass no maxMs, so the effective wait ceiling is 15 min; even a hint that reached it is clamped by Math.min(maxMs,…). This directly contradicts the docstrings (supervisor.mjs:5-9, 33-36: 'WAITS … until the limit resets, honoring a retry-after hint (a real reset time)') and the header (supervisor.mjs:4: 'a multi-hour reset'). Failure scenario holds: on a 1h Anthropic usage-window reset, the loop returns {stopReason:'review error on pass N: rate_limit…'} (string, resumable via isResumableStop, and does NOT match DID_NOT_RUN_RE so didNotRunStreak stays 0 and won't terminate). The supervisor waits ≤15 min, resumes ~45 min short of reset, re-fails, and repeats for the whole window — never honoring the reset, wasting resume cycles that each re-trigger failed backend calls. The retry-after path is exercised only by unit tests feeding {retryAfter:N} objects directly (supervisor.test.mjs:36-39), never by the real loop. This matches the project's explicitly-flagged anti-facade defect class (built + tested-in-isolation but never reached) and defeats the module's stated purpose on a common production event. All refutation attempts failed: no wrapper adds err/retryAfter; the loops never rethrow rate-limit errors so the err-object path is unreachable; there is no other wait computation besides supervisor.mjs:96/104. Severity is borderline P1/P2 (the run eventually recovers after the window resets, no data loss or wrong exit code) but I concur with P1 given the anti-facade elevation and defeat of the module's core reason for existing.

## 12. [P1] Branch-mode diff ignores git exit status -> a failing git diff silently reviews an EMPTY diff and reports the branch clean

- **File:** `plugins/council/scripts/lib/git-context.mjs:240` · **Category:** correctness
- **Detail:** In collectReviewContext branch mode (git-context.mjs:234-243) the shortstat, diff, and log are all run via runCommand(), whose non-zero exit status is discarded; only `.stdout` is read. assertBaseRef() only verifies the base commit EXISTS, it does not guarantee `git diff <base>...HEAD` will succeed. When the diff command fails after that check, `.stdout` is "" and the code renders content="(empty)", summary="(no branch diff)". The whole council/patch-gate then sees zero changes and treats the branch as clean -- a false negative that lets real changes bypass review. I reproduced the trigger: with `--base` pointing at a branch that has an unrelated history, `git diff base...HEAD` prints `fatal: base...HEAD: no merge base` and exits 128 with empty stdout (verified: EXIT=128, stdout_len=0). The same silent-empty outcome occurs on a malformed skipPaths pathspec (e.g. an unmatched `[`), a failing external `.gitattributes`/config diff driver, or an fsmonitor/core error. Working-tree mode is only partly protected: `git status` there uses runCommandChecked (throws), but the staged/unstaged diffs at git-context.mjs:218-219 are also unchecked runCommand calls, so a diff-driver/fsmonitor failure there likewise yields a silently empty diff while status still reports files as changed. The fix is to use runCommandChecked (or explicitly assert status===0) for the diff/shortstat commands so a git failure fails loud instead of masquerading as 'no changes'.
- **Failure scenario:** User runs review with --base=<ref> where <ref> is on an unrelated history (or supplies a malformed skipPaths glob). assertBaseRef passes (the base commit exists). `git diff base...HEAD -- . :(exclude,...)` fatals with exit 128 and empty stdout. runCommand swallows the non-zero status; diff="" and stat="". collectReviewContext returns summary='(no branch diff)' and content with 'diff (...)' => '(empty)'. The council reviews an empty diff, finds nothing, and the branch (which actually contains changes) passes the §6 gate / review as clean.
- **Verifier:** CONFIRMED real fail-open. In branch mode (git-context.mjs:237-243) `stat`, `diff`, and `log` run via runCommand(), which returns a `status` field (process.mjs:97) that the caller discards — only `.stdout` is read. assertBaseRef() (line 139-146) runs `git rev-parse --verify --quiet <base>^{commit}`, which only proves the base COMMIT exists; it does not guarantee `git diff <base>...HEAD` succeeds. Empirically verified: with `--base` on an unrelated history, `git diff master...HEAD` (three-dot) fatals with `no merge base`, EXIT=128, stdout_len=0, while `git rev-parse --verify --quiet master^{commit}` returns EXIT=0. Result: stat="" → summary="(no branch diff)", diff="" → rendered "(empty)". The reviewers (deliberate.mjs:253 → REVIEW_INPUT: context.content) receive an empty diff and find nothing, so a branch that actually contains changes passes review as clean. The intent to check is proven by asymmetry: working-tree mode uses runCommandChecked for `git status` (throws on failure), but the diff/shortstat calls — and the working-tree staged/unstaged diffs at 218-219 — use bare runCommand. No downstream guard saves it: the build path is fail-closed on empty diffs (build-step.mjs:797, structure-wiring.mjs:497) but the collectReviewContext consumer has no such veto, and even a `!content.trim()` check wouldn't fire because the two-dot commit log (which does NOT hit the merge-base error, verified EXIT=0/non-empty) keeps `content` looking populated while the actual diff is empty. The trigger generalizes beyond exotic histories: any git failure on the three-dot diff — a broken external diff driver via .gitattributes, an fsmonitor/config error — silently reviews a NORMAL base ref as clean. This is a silent fail-open in a system whose stated invariant is fail-closed review gating (anti-facade). Fix: use runCommandChecked (or assert status===0) for the shortstat/diff/log commands. P1: silent false-negative that lets real changes bypass review; not P0 only because it requires a git-command failure (uncommon base ref or environmental diff-driver/fsmonitor fault) rather than firing on every run.

## 13. [P2] findings/counter reducer silently drops entries keyed "__proto__" (bracket Set-assignment hits the __proto__ accessor)

- **File:** `plugins/council/scripts/lib/progress.mjs:131` · **Category:** data-integrity
- **Detail:** In the `findings` case the aggregated cell is stored with `byLens[lens] = cell` (line 131). When `lens` resolves to the literal string "__proto__", this is a `[[Set]]` assignment that invokes the inherited `__proto__` accessor instead of creating an own property: because `cell` is an object, it REPARENTS `byLens` (its prototype becomes `cell`) and creates no own "__proto__" key. Verified: `Object.getPrototypeOf(byLens) === cell` and `JSON.stringify(byLens) === "{}"`. So the finding is never recorded in the persisted per-lens matrix, yet the loop still ran `count += 1`, so a named seat's `raised` IS bumped (line 143). The matrix therefore under-counts by one relative to `seat.raised` and the finding is silently lost — directly contradicting the case's own comment "Every finding is counted even if its lens/severity is junk (bucketed to other/nit)." The lens is derived from `f.lens ?? f.category`, which for LLM-emitted findings is arbitrary attacker/garbage-influenced text, so "__proto__" is reachable. The identical class exists at line 101 (`counters[key] = ...`): `counter("__proto__")` assigns a NUMBER, the `__proto__` setter ignores non-object values, no own key is created, and the counter is silently dropped (`JSON.stringify` yields `{}`), violating "a field only appears once actually reported." No global prototype pollution occurs (Object.prototype is untouched) and fail-soft is preserved (no throw), but it is silent data loss plus a self-contradicting invariant. Fix: guard the key, e.g. use a null-prototype accumulator (`Object.create(null)` / `Map`) or reject/rename `lens`/`key === "__proto__"` before the bracket assignment.
- **Failure scenario:** mergeProgressEvent(state, { type: "findings", list: [{ lens: "__proto__", severity: "P0" }], seat: "codex" }): the loop sets count=1 and bumps seat "codex".raised to 1, but `byLens["__proto__"] = cell` reparents byLens instead of adding an own key, so next.findingsByLens serializes to `{}` and progress.json shows zero findings for that lens while seat codex.raised=1 — the finding vanishes and seat.raised no longer matches the matrix total. Same with counter("__proto__"): the counter never appears in progress.json.
- **Verifier:** Confirmed by running the actual exported reducer. mergeProgressEvent(state,{type:"findings",list:[{lens:"__proto__",severity:"P0"}],seat:"codex"}) makes findingsByLens serialize to {} (the finding is dropped) while seats[0].raised becomes 1 — byLens is reparented via the Object.prototype.__proto__ setter and no own "__proto__" key is created (verified: byLens own keys=[], proto!==Object.prototype, round-tripped persisted state shows findingsByLens:{}). A normal lens ("correctness") produces {correctness:{total:1,...}} with raised=1 that agree, proving the divergence is specific to "__proto__". Same at line 101: counter("__proto__",3) leaves counters serializing to {} because the setter ignores the numeric value. This silently drops the finding/counter and desyncs seat.raised from the matrix total, directly violating the code's own line-119 invariant "Every finding is counted even if its lens/severity is junk." Reachability: the counter half is NOT production-reachable (all counter keys are hardcoded literals reverted/proposed/fixed/committed), but the findings half IS — audit-review.mjs:379 folds r.merged.all whose `category` comes straight from LLM output (schema audit-review.mjs:136) and the reducer falls back to f.category, so an LLM emitting or being prompt-injected into category:"__proto__" hits line 131. Blast radius is telemetry-only: the real audit findings use separate channels; only the live dashboard per-lens matrix under-counts and desyncs. Fail-soft is preserved (no throw), no global Object.prototype pollution (({}).__proto__ still === Object.prototype), and no command outcome/exit-code change — hence P2, not P0/P1. Fix: use Object.create(null)/Map for byLens and counters, or reject/rename key==="__proto__" before the bracket assignment.

## 14. [P2] Per-unit progress never advances for failed/empty units and is never emitted on the catch path — bar stalls below 100%

- **File:** `plugins/council/scripts/lib/audit-review.mjs:377` · **Category:** telemetry-correctness
- **Detail:** In runAuditReview's worker (lines 361-384), reporter.progress is only called inside the try, AFTER a successful reviewUnit, and the reported count is done = results.filter(x => x.reviewed).length. Two gaps: (1) A unit whose reviewUnit rejects (Promise.all in reviewUnit rejects the moment any seat runner throws — line 241) is caught and pushed to `failed` with NO reporter.progress emit at all. (2) A unit that returns but where every seat came back unparseable/empty has reviewed:false, so it is never counted in `done` even though it was processed. Because unitsDone counts only successfully-reviewed units and is never emitted after a failure, the final persisted progress.json for the review phase can permanently show unitsDone < unitsTotal. This is exactly what Grok flagged, and it diverges from the correct reference in the sibling grouped path (audit-grouped-review.mjs:136-138) where onCell does `cellsDone += 1; reporter.progress(...)` for EVERY completed cell regardless of ok/failure, so that bar always reaches total. Fail-soft telemetry invariant is respected (outcome/exit code unchanged; the returned coverage counts are correct), so this is a dashboard-accuracy defect, not an outcome change.
- **Failure scenario:** 12 hotspot units selected; the last-completing unit's reviewUnit rejects because one seat runner throws (e.g. a transient spawn error) -> caught at line 380, pushed to `failed`, no progress emitted. The last progress written shows unitsDone:11 (or fewer) of 12. The live dashboard shows the review permanently stuck at 11/12. Same visible stall if any unit's seats all return empty/unparseable (reviewed:false).
- **Verifier:** Confirmed real, P2 telemetry-accuracy defect. In runAuditReview's worker (audit-review.mjs:361-384), per-unit progress is emitted only on the try-success path and the emitted count is done = results.filter(x => x.reviewed).length (line 377). reviewUnit sets reviewed:false whenever no seat returned status===0 with parseable JSON (line 266) — a normal, common outcome (all seats time out / error / return garbled JSON). Such a unit still resolves normally (no throw), takes the success path (results.push at 374), and is excluded from `done`. Therefore the maximum unitsDone ever emitted equals the reviewed count; if ≥1 selected unit ends reviewed:false, the final unitsDone < unitsTotal. progress events shallow-merge into state.progress (progress.mjs:109-114) and reporter.done() (council-companion.mjs:2067) never touches state.progress, so progress.json permanently records e.g. unitsDone:11/unitsTotal:12, and the dashboard renders the bar unconditionally from those values (watch.mjs:621,670) even in the done state — a bar stuck below 100%. The reporter is genuinely wired (council-companion.mjs:2057/2063), not a facade. The sibling grouped path (audit-grouped-review.mjs:136-138) advances cellsDone unconditionally per completed cell, establishing the intended "bar reaches total" semantics that the per-file path violates. Correction to the claim: gap (1)'s cited trigger (a transient spawn error making reviewUnit reject) is wrong — runCommandAsync swallows all spawn/child errors into status:1 (process.mjs:142,182,272-274), so a spawn error produces reviewed:false (gap 2), not the catch path; the catch path emits no progress but is only reached by a genuine throw inside a runner (rare). Gap (2) alone fully substantiates the reported stall. No outcome/exit-code change; returned coverage counts (unitsReviewed/unitsFailed) are correct, so this is dashboard-only.

## 15. [P2] globalReduce findings are never folded into the live per-lens matrix (reporter.findings only ever gets per-unit r.merged.all)

- **File:** `plugins/council/scripts/lib/audit-review.mjs:391` · **Category:** facade-telemetry
- **Detail:** reporter.findings is called exactly once, per unit, with r.merged.all (line 379). After the worker loop, globalReduce runs (line 391) and its findings are merged into the returned result via `...reduce.all` (line 397 -> scoped.all -> return.findings), but reduce.all is NEVER passed to reporter.findings. For a single `audit review` or single-shot `audit fix` run, council-companion.mjs threads the real reporter directly into runAuditReview (lines 2057-2064 and 2407), so the dashboard's findingsByLens matrix permanently omits every finding the global SSOT/architecture reduce produced — precisely the cross-cutting ssot/architecture/design findings (AUDIT_REDUCE_TEMPLATE categories). This is Grok's second claim, confirmed. The endless loop happens to re-fold the whole run via reporter.findings(fresh) at audit-endless.mjs:145 with a DIFFERENT reporter (the endless review closure at council-companion 2544 does not pass the endless reporter down), so the gap is invisible there but real for the single-run dashboards. Outcome/return value is correct (reduce findings ARE in `findings`); only the live matrix is wrong.
- **Failure scenario:** Operator runs `audit review` (non-endless) and watches the live dashboard. globalReduce returns 3 SSOT/architecture findings; per-unit reviews return bug/security findings. reporter.findings is invoked only for the per-unit findings, so findingsByLens shows the bug/security lenses but zero ssot/architecture — the dashboard understates total findings and hides the entire structural category, even though the final report (out.findings) includes them.
- **Verifier:** Confirmed real. In runAuditReview (plugins/council/scripts/lib/audit-review.mjs), reporter.findings is called exactly once — line 379, inside the worker loop, with per-unit r.merged.all. globalReduce runs afterward (line 391); its reduce.all is folded ONLY into the returned findings (lines 397/403/443), never into reporter.findings. A grep confirms line 379 is the sole reporter.findings call in the file, and there is no reporter.findings(reduce.all) after the reduce nor any post-return reconciliation in council-companion.mjs (after runAuditReview returns at lines 2065/2408, only reporter.done({ok:true}) fires, which per progress.mjs done-case touches no findings). progress.mjs (lines 115-134) builds the live findingsByLens matrix solely from reporter.findings events, keying by f.lens ?? f.category. The reduce template's category enum is ssot|architecture|dead-code|design (line 158-159) while the per-unit template's enum (line 136) has NO ssot/architecture — so those cross-cutting rows are UNIQUE to the reduce and are permanently absent from the live matrix, and totals are undercounted. council-companion.mjs threads the REAL reporter directly into runAuditReview for both non-endless `audit review` (lines 2057-2065) and single-shot `audit fix` (line 2407), so both dashboards are affected. The endless path is genuinely immune as claimed: its review closure (lines 2544-2556) does not pass reporter down (so line 379 hits NOOP), but audit-endless.mjs:145 re-folds `fresh` — which comes from res.findings including pass-1 reduce findings — via the real endless reporter. Failure scenario is concrete and reproducible: operator runs non-endless `audit review` on a repo with duplicate clusters/cycles; globalReduce emits ssot/architecture findings that appear in the final report (out.findings) but the /council:watch findingsByLens matrix shows zero ssot/architecture rows and understates the total. P2 is correct: the command outcome, exit code, and final report are all correct; only the live telemetry matrix is wrong (additive/fail-soft invariant on outcome is not violated, but the dashboard is inaccurate and hides an entire finding category).

## 16. [P2] Successfully applied structural fixes are double-counted: 'proposed' is never decremented when a structure finding is promoted to fixed/committed

- **File:** `plugins/council/scripts/lib/audit-fix.mjs:912` · **Category:** reporter-accounting
- **Detail:** Line 608 counts EVERY entry currently in `rejected` as 'proposed' in one shot: `if (rejected.length) reporter.counter("proposed", rejected.length)`. Structural findings (lens architecture_ssot / logical_sense, confirmed via structure-gate.mjs isStructureClass) are cross-cutting, so classifyFixable already pushed them into `rejected` before line 608 — they ARE included in that 'proposed' total. Later the structure pass (lines 893-919) applies them: on success it `rejected.splice(i,1)` (910), `fixed.push(...)` (911) and bumps `reporter.counter("fixed")` + `reporter.counter("committed")` (912-913). The reporter counters are monotonic increments (progress.mjs mergeProgressEvent 'counter' case, no decrement), and nothing ever decrements 'proposed'. So a structural finding that gets auto-applied is counted BOTH as 'proposed' AND as 'fixed'/'committed'. This is exactly the 'structure-success double-counts proposed' that Grok flagged. It only affects the additive/fail-soft telemetry (never the command outcome), but the live dashboard/watchers read these counters and will misrepresent the run.
- **Failure scenario:** options.structureAutoApply === true with deps.runStructureTransform injected and 2 structural (cross-cutting) findings that the structure pass applies successfully (§6 unanimous, tests green), no other findings. classifyFixable rejects both -> rejected=[f1,f2], tasks=[]. Line 608 fires reporter.counter('proposed', 2) -> proposed=2. Structure pass splices both out of rejected and bumps fixed/committed -> fixed=2, committed=2. Final persisted counters: proposed=2, fixed=2, committed=2, even though ZERO findings remain as proposals. Correct state is proposed=0, fixed=2. The dashboard shows 2 proposals + 2 fixes for the same 2 findings.
- **Verifier:** CONFIRMED. Structural (cross-cutting) findings are rejected by classifyFixable (ineligibleReason line 152: scope !== "localized" → propose-only) and thus sit in `rejected` when line 608 runs `if (rejected.length) reporter.counter("proposed", rejected.length)` — so they are counted as 'proposed'. The counter merge in progress.mjs (line 101: counters[key] = existing + delta) is strictly additive with no decrement. The M9 structure pass (lines 909-914) then applies them: it splices out of the returned `rejected` array (910, correct) and bumps reporter.counter("fixed")+("committed") (912-913), but never decrements 'proposed'. A grep of all reporter.counter calls (604/608/648/656/675/855/856/912/913) confirms no 'proposed' decrement exists anywhere. So a structural finding that is auto-applied is counted BOTH as 'proposed' AND as 'fixed'/'committed'. The claimed scenario holds exactly: structureAutoApply===true + runStructureTransform injected + 2 cross-cutting structural findings, no other findings → structuralPending keeps line 574 from early-returning → line 608 sets proposed=2 → structure pass sets fixed=2, committed=2. Final telemetry: proposed=2, fixed=2, committed=2 for 2 findings, all fixed; correct state is proposed=0, fixed=2. The returned result object (rejected=[], fixed=2) is correct — only the reporter telemetry is wrong. watch.mjs (COUNTER_KEYS line 421, render lines 628/699) displays each counter as an independent labeled total ("proposed 2 / fixed 2 / committed 2"), so the live dashboard visibly misrepresents the run. Refutations fail: the single-file applied path (855-856) does NOT double-count because those findings were eligible (absent from rejected at line 608); the per-finding proposed bumps (648/656/675) are for eligible-then-rejected findings and don't overlap line 608. The bug is specific to the structure pass exactly as claimed. Telemetry-only, non-outcome-affecting (respects the additive/fail-soft invariant), so P2 is appropriate.

## 17. [P2] §6 council gate is left stuck in 'running' state on every error/drift revert path (no terminal veto/pass emitted)

- **File:** `plugins/council/scripts/lib/audit-fix.mjs:804` · **Category:** gate-state
- **Detail:** Line 789 emits `reporter.gate({ name: "§6-council", state: "running" })` on entry to the §6 block. A terminal state is only emitted on two of the exits: line 812 (`state: "veto"` when not unanimous) and line 848 (`state: "pass"`). The other fail-closed exits that revert-and-continue do NOT emit any terminal gate state: line 794 (no diffText -> revert), line 804 (reviewPatch threw -> revert), line 824 (changed-set drifted during review -> revert), line 837 and line 844 (staged/working-tree reviewed-bytes drift -> revert). progress.mjs 'gate' reducer merges a patch or clears on null; there is no auto-clear, and mergeProgressEvent 'done' does not touch `gate`. So after any of these reverts the persisted gate stays {name:'§6-council', state:'running'} until (if ever) the NEXT sensitive finding re-enters the block. When the last gate-touching finding takes one of these paths, the run finishes with the §6 gate frozen at 'running'. Semantically these ARE vetoes (fail-closed -> propose-only) and should report a terminal state like line 812 does. Telemetry-only (does not change outcome), but the dashboard shows a council gate perpetually 'running' after the run has already finished and reverted the fix.
- **Failure scenario:** A single sensitive-class finding under consented sensitiveAutoApply (reviewPatch injected). Mechanical gates + tests pass, §6 block emits gate 'running' (789), then reviewPatch throws a non-rate-limit error (or retries exhaust). Catch at 802-807 reverts and `continue`s without emitting a terminal gate state. Loop ends, reporter.done() fires. progress.json final `gate` = {name:'§6-council', state:'running'} — the live dashboard renders the §6 council gate as still running even though the run is done and the fix was reverted to propose-only. Same outcome for the changed-set-drift (824) and staged-diff-drift (837/844) reverts.
- **Verifier:** Verified and confirmed. In audit-fix.mjs the §6-council gate emits 'running' on entry (789) but a terminal state only at 812 (veto) and 848 (pass). The fail-closed revert-and-continue paths at 792-797 (no diffText), 800-807 (reviewPatch threw), 822-827 (changed-set drift), 835-840 and 842-847 (staged/working-tree diff drift) each revert + continue WITHOUT emitting any terminal gate state, so the last-emitted 'running' persists. progress.mjs confirms no auto-clear: the 'gate' reducer (105-108) only clears on explicit null/merges a patch, and the 'done' reducer (162-168) never touches next.gate, so the frozen 'running' survives to the terminal snapshot. watch.mjs renders the gate unconditionally via hasGate (542) in both ASCII (625-626) and markdown (696-697) with no suppression when done, and GATE_EMOJI.running='🟡' (423). Reachability is real (anti-facade check passes): reviewPatch is wired in production via makePatchReviewer in council-companion.mjs (2228-2285) and required by sensitiveAutoApply (audit-fix.mjs:493). Concrete failure: a single sensitive-class finding whose reviewPatch throws a non-limit error hits the catch at 802-807, reverts to propose-only, continues with no terminal gate; the loop ends and reporter.done() fires, leaving progress.json gate={name:'§6-council',state:'running'}; the dashboard then shows the council gate perpetually running after the run has finished and reverted the fix. Same outcome for the drift reverts when the last §6-entering finding takes one of them. It is telemetry-only (outcome unchanged — the fix is correctly reverted, consistent with the additive-telemetry invariant), but it is a genuine semantic inaccuracy, not a style nit: these paths are effectively vetoes and should emit a terminal gate state like line 812 does.

## 18. [P2] Fix-loop budget is reported PRE-charge and never re-emitted, so progress.json under-reports spend (a 1-pass run shows 0 spent)

- **File:** `plugins/council/scripts/lib/audit-fixloop.mjs:219` · **Category:** telemetry-correctness
- **Detail:** In runFixLoop the only `reporter.budget(...)` call is line 219, emitted at the START of each pass BEFORE that pass charges any review/fix spend (the `charge(...)` calls happen later at lines 241 and 274). There is no post-charge budget emit, and the loop never calls reporter.budget again after the for(;;) loop. This is the exact opposite ordering of the sibling loop runEndless, which emits `reporter.budget(spent, totalBudget)` AFTER charging (audit-endless.mjs:144). I grep-confirmed the review/fix sub-deps (runAuditReview / runGroupedReview / runAuditFix) never emit `.budget` themselves, so line 219 is the sole source. Consequences: (1) the live progress.json / watch dashboard budget always lags one full pass and NEVER reflects the final pass's spend; (2) a run that stops after pass 1 emits only budget(0, totalBudget), so the persisted budget.spent stays 0 even though the pass consumed real agent calls. The fail-soft invariant is preserved (the run's outcome and the returned out.spent are correct), but the telemetry the user watches is materially wrong.
- **Failure scenario:** Run `audit fix --loop` with a real makeProgressReporter. Case A (single pass): pass 1's review spends 10 and fix returns integrationFailed → the loop breaks (line 267). reporter.budget was only ever called with (0, totalBudget), so progress.json persists budget.spent=0 while out.spent≈10 and the live Budget bar reads 0/60 for a run that spent real calls. Case B (multi-pass, e.g. the provided progress test: budget 40, converges in 3 passes): out.spent=8 but the last reporter.budget emit was at the start of pass 3 with spent=5, so the terminal progress.json shows budget.spent=5 (last pass's charge never emitted) — an under-count vs the true 8/40.
- **Verifier:** CONFIRMED. In runFixLoop (audit-fixloop.mjs) reporter.budget(spent,totalBudget) is emitted ONLY at line 219, at the START of each pass, BEFORE that pass charges review spend (line 241) and fix spend (line 274). There is no post-charge or post-loop budget emit: after the for(;;) loop the CLI only calls reporter.done() (council-companion.mjs:2333), which in mergeProgressEvent (progress.mjs case "done") never touches state.budget. A repo-wide grep for `.budget(` returns only audit-endless.mjs:144, audit-fixloop.mjs:219, and tests — so runAuditReview/runAuditFix never emit budget, making line 219 the sole feeder of progress.json's budget field. The sibling runEndless does it correctly: charge (line 141) THEN reporter.budget (line 144). Consequences verified by tracing the existing progress test (budget 40, 3-pass convergence): final out.spent=10 but the last emit fired at pass-3 start with spent=7, so terminal progress.json persists budget.spent=7 (under-count); a single-pass run that breaks after review charged (integrationFailed at line 267/271) emits only budget(0,total), so progress.json shows spent=0 for a run that consumed real agent calls. The existing test asserts only snap.budget.total===40, never budget.spent, so the gap is untested. Fail-soft invariant is preserved (out.spent/outcome/exit code correct), so this is telemetry-accuracy only — but it is a real, specific, reproducible defect that makes the /council:watch budget bar materially wrong, with a clean correct-reference sibling. P2.

## 19. [P2] reporter.done hardcodes ok:true even when the endless run ended on a review-error stopReason

- **File:** `plugins/council/scripts/council-companion.mjs:2570` · **Category:** telemetry-correctness
- **Detail:** council-companion.mjs:2570 calls `reporter.done({ ok: true, stopReason: out.stopReason })` unconditionally. runEndless can terminate with a FAILURE stopReason: on a thrown pass it sets `stopReason = review error on pass N: …` and breaks (audit-endless.mjs:132-136), returning findings that may be empty. In progress.mjs the done reducer (lines 162-168) sets `ok` verbatim and `phase = e.ok === false ? 'failed' : 'done'`, so ok:true forces phase 'done'. A crashed endless run is therefore persisted to progress.json as ok:true / phase 'done'. This contradicts the sibling paths that derive ok from the outcome — fixloop `reporter.done({ ok: !out.stranded })` (2333) and audit review `reporter.done({ ok: out.ok !== false })` (2463). runEndless returns no `ok` field, so the caller must derive it (e.g. `ok: !/^review error/.test(String(out.stopReason ?? ''))`) or runEndless should return an explicit failed flag. It does NOT change the process exit code (endless sets none), so the additive-telemetry invariant is upheld, but the dashboard/automation signal is wrong.
- **Failure scenario:** Every review pass throws (all reviewer CLIs crash or hard rate-limit). runEndless breaks on line 134 with stopReason 'review error on pass 1: …' and findings []. council-companion.mjs:2570 writes progress.json with ok:true, phase 'done'. A watcher or downstream gate keying on progress.json `ok` treats the fully-failed run as a clean success.
- **Verifier:** Confirmed real. council-companion.mjs:2570 calls reporter.done({ ok: true, stopReason: out.stopReason }) unconditionally for the endless path. runEndless (audit-endless.mjs:120-162) can terminate on a FAILURE stopReason: when a review pass throws, lines 130-136 catch, set stopReason = "review error on pass N: …", and break; line 162 returns no `ok` field, and if pass 1 throws, findings is []. In progress.mjs:164-167 the done reducer sets next.ok = e.ok ?? null → true and next.phase = e.ok === false ? "failed" : "done" → "done", persisting ok:true/phase:"done" to progress.json for a crashed run. There IS a concrete consumer: watch.mjs:505-524 computes failed = s.ok === false || phase === "failed" → false, then status: failed ? "failed" : done ? "done" : "running" → "done" — so a fully-crashed endless review renders as clean success ("done") in the live /council:watch dashboard, not "failed". The sibling paths correctly derive ok from outcome (fixloop 2333: ok:!out.stranded; single-shot fix 2463: ok: out.ok !== false), so the endless path is a genuine inconsistency. The total-crash scenario is reachable — the activeReviewerCount guard at 2497 only checks reviewers exist at start, not that a call survives (CLI crash / hard rate-limit throw is exactly what the try/catch at line 130 handles); nothing downstream re-derives ok from stopReason. Severity P2, not P1: the additive-telemetry invariant is upheld (endless sets no exit code, so the command outcome/exit code is unchanged), no data loss, and stopReason still carries the crash text for a human — but the machine-readable ok/status signal a watcher or downstream gate keys on is wrong. Fix: derive ok in the caller (e.g. ok: !/^review error/.test(String(out.stopReason ?? ''))) or have runEndless return an explicit failed flag.

## 20. [P2] Resume never reseeds the per-pass `passes` log, so a resumed run's report/checkpoint undercounts passes

- **File:** `plugins/council/scripts/lib/audit-endless.mjs:102` · **Category:** resume-data-loss
- **Detail:** The resume block (audit-endless.mjs:108-118) reseeds all/spent/passNo/dryStreak from the checkpoint but leaves `passes` at its fresh `[]` (declared line 102). passNo IS restored, so the loop continues numbering correctly, but `passes` only accumulates THIS run's entries. The return object (line 162) then has passesRun=N (correct) while passes.length is only the post-resume subset, and the final checkpoint write (line 161) OVERWRITES the persisted `passes` with just the post-resume entries — discarding the prior run's per-pass log for good. Fix: seed `passes.push(...prior.passes)` (guarded by Array.isArray) in the resume block alongside findings.
- **Failure scenario:** `audit endless` runs 3 passes then is interrupted (checkpoint holds passes[1..3], passNo 3). `audit endless --resume` seeds passNo=3 but passes=[]; it runs passes 4-5 and returns passesRun=5 with passes=[pass4,pass5]. renderAuditEndlessReport (council-companion.mjs:2798-2803) prints 'Ran 5 pass(es)' but the '## Passes' list shows only pass 4 and 5, and the final checkpoint now persists only [pass4,pass5], permanently dropping the pass 1-3 log.
- **Verifier:** Confirmed real. In audit-endless.mjs the `passes` array is declared fresh at line 102, and the resume block (108-118) reseeds all/spent/passNo/dryStreak from the checkpoint but never reseeds `passes`. Because passNo IS restored, the return object has passesRun=cumulative (line 162) while `passes` holds only the post-resume entries — the two necessarily disagree after any resume. renderAuditEndlessReport (council-companion.mjs:2798-2803) receives `out` directly and prints "Ran N pass(es)" from out.passesRun but lists only out.passes, so it under-lists; the --json path (2576) emits the same undercounted array. Worse, the final checkpoint write (audit-endless.mjs:161) overwrites the persisted `passes` with only the post-resume subset, permanently discarding the prior run's per-pass log (data loss of the audit trail). Decisive corroboration: the sibling module audit-fixloop.mjs:152 does exactly the proposed fix — `if (Array.isArray(prior.passes)) passes.push(...prior.passes);` — proving this reseed is the established convention that audit-endless omits by oversight. Scope is correctly P2: findings (`all`) and control flow (`passNo`) are restored fine, so actual audit results and stop conditions are unaffected; only the per-pass log/report and persisted audit trail are wrong. If a resumed passNo is already >= maxPasses, the loop breaks immediately and `passes` stays [], wiping the log entirely.

## 21. [P2] onPhase wrapper does not swallow a throwing progress hook — a telemetry throw can abort an otherwise-successful deliberation

- **File:** `plugins/council/scripts/lib/plan-deliberate.mjs:622` · **Category:** telemetry-fail-soft
- **Detail:** In runPlanDeliberation the progress seam is:

  const onPhase = (msg) => {
    reporter.phase("plan", String(msg));
    userPhase(msg);
  };

Neither call is wrapped in try/catch. `reporter.phase` from the shipped makeProgressReporter is internally fail-soft, so it won't throw — but `userPhase` (= options.onPhase, the legacy stderr progress sink) is a caller-supplied hook that is invoked unguarded. The design invariant is explicit: telemetry is additive + fail-soft and 'a write/clock/hook throw must be swallowed' — telemetry must NEVER change a command's outcome/exit code. The comment on this very wrapper frames onPhase as telemetry ('both notifies the caller and drives the live progress phase'). The codebase's own standard confirms this: progress.mjs wraps its logSink in try/catch precisely because 'a broken sink must not break the command' (reporter.line, lines 273-278). The analogous stderr sink here (userPhase) is not guarded. If userPhase throws, onPhase throws, the exception propagates out of runPlanDeliberation, and a read-only plan deliberation that could have completed instead rejects with a nonzero exit — a telemetry hook changing the command outcome, which the invariant forbids. (Same unguarded pattern exists in deliberate.mjs lines 244-250, so this is systemic, but it is a real defect in this file.)
- **Failure scenario:** A programmatic embedder calls runPlanDeliberation with options.onPhase = a phase listener that throws (e.g., bookkeeping that hits an error), or the CLI's console.error sink throws (broken/redirected stderr, EPIPE/ENOSPC). On the first milestone (onPhase('proposals (N)') at line 661) userPhase throws -> onPhase throws -> runPlanDeliberation rejects before any seat call. A best-effort progress notification has flipped the command's exit code from success to failure, violating the additive/fail-soft telemetry invariant. Fix: wrap the userPhase(msg) call (and, defensively, reporter.phase) in try/catch that swallows, matching reporter.line's guarded logSink.
- **Verifier:** Confirmed. In runPlanDeliberation (plan-deliberate.mjs:620-623) the onPhase wrapper calls userPhase(msg) (=options.onPhase) unguarded; reporter.phase IS fail-soft (mergeProgressEvent phase branch only assigns; persist/safeNow are try/caught), so userPhase is the sole unguarded limb. onPhase fires synchronously at line 661 before any seat call, so a throwing hook rejects the whole read-only deliberation, flipping success->failure. This violates the stated fail-soft invariant, which explicitly says a 'hook throw must be swallowed', and contradicts the codebase's own precedent: reporter.line (progress.mjs:273-279) wraps its analogous logSink in try/catch ('a broken sink must not break the command'). The unguarded userPhase is exactly that sink, unwrapped. Severity is correctly P2 (not higher) because the only shipped caller (council-companion.mjs:2974) passes (m)=>console.error(m) on the global console, which has ignoreErrors=true and won't throw synchronously — so the CLI path is safe by accident. But runPlanDeliberation is an exported public seam; any programmatic caller supplying an onPhase that can throw (a bookkeeping listener, or a non-global-console/ignoreErrors=false sink) gets a successful deliberation aborted by a best-effort progress notification. Concrete failure: runPlanDeliberation(cwd, req, backends, {onPhase: () => { throw new Error(); }}) rejects at the line-661 'proposals' milestone before any seat runs. Fix: wrap userPhase(msg) (and defensively reporter.phase) in a swallowing try/catch, matching reporter.line. Same unguarded pattern exists in deliberate.mjs:247-250 (systemic), but it is a real defect in this file.

## 22. [P2] verification.verifiedCount reports findings SENT to refutation, not survivors — dashboard/metrics overstate 'verified'

- **File:** `plugins/council/scripts/lib/deliberate.mjs:508` · **Category:** telemetry-correctness
- **Detail:** deliberate.mjs line 508 surfaces `verification = { verifiedCount: vr.verifiedCount, refutedCount: vr.refutedCount }`. But `vr.verifiedCount` is `targets.length` from verify.mjs (verifyFindings passes `targets.length` into partitionByRefutation, which returns it verbatim as `verifiedCount`). `targets` is every P0/P1 non-consensus finding that shouldVerify() selected — it INCLUDES the findings that were subsequently refuted, and even findings for which verifierFor() returned no reachable non-authoring seat (never actually checked). This value is consumed as `verified` by metrics.mjs:59 (`verify: { verified: d.verification.verifiedCount, refuted: d.verification.refutedCount }`) and by the live watch dashboard (watch.mjs:278), where it reads as 'findings that survived verification' shown side-by-side with `refuted`. Because refuted ⊆ verified in this count, the two numbers double-count and the 'verified' figure overstates how many findings actually held up. It is telemetry-only (additive, does not change the review outcome or which findings surface), hence P2, but it is a concrete wrong number displayed to the user and recorded in metrics history.
- **Failure scenario:** A deliberate run with --verify has 4 unique P1 findings (non-consensus). All 4 are checked; an independent verifier refutes 3, 1 survives. verifyFindings returns verifiedCount=4 (targets.length), refutedCount=3. deliberate returns verification={verifiedCount:4, refutedCount:3}; metrics.mjs and the watch dashboard show 'verified: 4, refuted: 3', implying 4 findings passed verification when in reality only 1 survived and 3 were refuted.
- **Verifier:** CONFIRMED. verify.mjs:153 passes `targets.length` as `verifiedCount` into partitionByRefutation, which (verify.mjs:163-189) returns it verbatim with no subtraction. `targets` (verify.mjs:113) is every non-consensus P0/P1 finding SENT to verification — it includes findings that were subsequently refuted AND findings for which verifierFor() returned null (no reachable non-authoring seat) that were never actually checked. deliberate.mjs:508 surfaces this as verification.verifiedCount; metrics.mjs:59 and watch.mjs:278 map it to `verify.verified`; watch.mjs:344 renders it as `${verified} held up · ${refuted} refuted under adversarial check`. Because refuted findings all originate from `targets`, refutedCount ≤ verifiedCount and the refuted set is double-counted (present in both the 'held up' figure and the 'refuted' figure). Scenario verified: 4 non-consensus P1 targets, 3 refuted, 1 survives → verifiedCount=4, refutedCount=3 → dashboard shows '4 held up · 3 refuted', which is self-contradictory and overstates survivors (true survivors = 1). Telemetry-only (additive, fail-soft, does not change review outcome or which findings surface), hence P2, but a concrete wrong number shown to the user and written to metrics history. The 'held up' count should be survivors (targets checked and not refuted), not targets.length.

## 23. [P2] A throwing audit/plan/build run leaves progress.json done:false forever (no try/finally), hanging `council watch` for the full --timeout

- **File:** `plugins/council/scripts/council-companion.mjs:2067` · **Category:** correctness
- **Detail:** `makeRunReporter` persists progress.json with `done:false` immediately (progress.mjs line 285), and the terminal `done:true` is written ONLY by the success-path `reporter.done()` call. At every call site the reporter is block-scoped inside its handler and `reporter.done()` sits as a plain statement AFTER the awaited work with no try/finally: audit-review (line 2067, awaits runAuditReview/runGroupedReview at 2057/2045), audit-fix-loop (2333, awaits runFixLoop/runSupervised at 2318), audit-fix (2463), audit-endless (2570, awaits runEndless at 2563), plan (2976, awaits runPlanDeliberation at 2969), build (3525, awaits runBuild at 3520). If the awaited work throws, `reporter.done()` is never reached and progress.json stays `done:false` until the next council long-running command in that workspace overwrites it. Unlike the deliberate path (executeCouncilReview's outer catch at lines 663-680 upserts a `failed` council job whose fresher updatedAt makes pickFreshestWatchSource pick the job and end the watch), audit/plan/build register NO council job, so nothing shadows the stale progress.json. `watchProgress` (line 1819-1829) uses `prog?.done` as its ONLY terminal condition, so it never exits until the deadline. Root cause / fix: wrap the awaited work in try/finally (or catch) and call `reporter.done({ ok:false, stopReason })` on throw; the reporter is fail-soft so a finally-call is safe.
- **Failure scenario:** Run `council audit review --groups fine`; makeRunReporter writes progress.json {kind:'audit-review', done:false}. runGroupedReview throws mid-run (git error, agent spawn failure, or any unexpected exception). The throw propagates out of handleAudit, line 2067 reporter.done() is skipped, the process exits non-zero, and progress.json is left done:false. The user then runs `council watch` (no job id) in a TTY: handleWatch reads the stale progress (readProgressState), latestId is null or an older council job, so pickFreshestWatchSource returns {kind:'progress'} (progTs >= jobTs) and calls watchProgress. The TTY loop re-reads the same done:false state every 2s; prog.done is never true, so it spins until Date.now() >= deadline (default --timeout 3,600,000ms). After a full hour it prints 'watch timed out after 3600s (progress still running)' and exits 1 — falsely reporting a dead run as live for an hour.
- **Verifier:** Confirmed. makeProgressReporter persists progress.json with done:false and a fresh updatedAt immediately (progress.mjs: initialProgressState done:false line 51, updatedAt line 40, persist() line 285). The terminal done:true is written only by the success-path reporter.done() statements, and every one of the six call sites (2067 audit-review, 2333 fix-loop, 2463 single-shot fix, 2570 endless, 2976 plan, 3525 build) has the awaited work followed by a plain reporter.done() with NO try/finally/catch. main()'s top-level catch (3621-3624) only prints the error and sets exitCode=1 — it never resets progress.json or calls reporter.done(). audit/plan/build register no council job (they use only makeRunReporter), so nothing shadows the stale file — unlike the deliberate path, whose executeCouncilReview try/catch (661-680) upserts a failed job with fresh updatedAt on throw. watchProgress (1819-1829) terminates only on prog?.done or the deadline; there is no staleness/PID-liveness check. pickFreshestWatchSource (watch.mjs 792-801) returns {kind:'progress'} when progTs >= jobTs, and a just-written progress.json beats an older/absent job. So after any mid-run throw from runGroupedReview/runAuditReview/runFixLoop/runBuild/runPlanDeliberation, a subsequent no-id TTY `council watch` reads done:false forever and hangs for the full --timeout (default 3,600,000 ms), then falsely prints 'progress still running' and exits 1. Adversarial refutation attempts all fail: no wrapping try/finally, no progress reset in the top-level catch, no liveness escape in watchProgress, no failed-job to shadow it. The one self-heal (next long-running command overwriting the file) does nothing for a watch invoked in the interim. I rate it P2 rather than the claimed P1: it is a genuine misleading-monitor + long-hang defect in a first-class UX surface with the fix pattern already present elsewhere, but it does not corrupt data, alter the audit command's own correct exit code, or breach the §6 gate, and it is conditional and recoverable (Ctrl-C; next run overwrites). Fix: wrap the awaited work in try/finally and call reporter.done({ok:false, stopReason}) on throw (the reporter is fail-soft, so a finally-call is safe).

## 24. [P2] `audit endless` reporter.done hardcodes ok:true, masking error/terminal stops on the live dashboard

- **File:** `plugins/council/scripts/council-companion.mjs:2570` · **Category:** correctness
- **Detail:** In the endless handler, `reporter.done({ ok: true, stopReason: out.stopReason })` hardcodes ok:true regardless of how the loop actually terminated. This is inconsistent with the two sibling handlers in the same slice, which correctly derive ok from the outcome: `plan` uses `ok: Boolean(out?.planSpec)` (line 2976) and `build` uses `ok: out?.ok === true` (line 3525). runEndless (lib/audit-endless.mjs:134) sets stopReason to `"review error on pass N: ..."` when a review pass throws, breaks the loop, and returns early; the `--supervise` path (lib/supervisor.mjs) can additionally return terminal stops such as "max attempts reached", "wall-clock cap reached", or "terminal — the review could not run N attempts in a row (likely permanent quota/outage)". progress.mjs's done reducer computes `next.phase = e.ok === false ? "failed" : "done"` and stores `next.ok = e.ok`, so with the hardcoded true the persisted progress.json (the universal contract read by `council watch`) shows phase:"done", ok:true for a run that actually errored out or was force-terminated. The printed console report (renderAuditEndlessReport) still shows the real "Stopped: <stopReason>", so this is a telemetry-accuracy defect confined to the dashboard channel — it does not change the command's exit code or findings — but it defeats the dashboard's job of surfacing failure and contradicts the codebase's own 'make gaps as visible as passes' discipline. Correct value would reflect the stop reason, e.g. ok:false when stopReason begins with 'review error' (and, on the supervise path, when the run ended on a supervisorStop that is not normal convergence).
- **Failure scenario:** Run `council audit endless`. On pass 3 the reviewer subprocess errors and runAuditReview throws; runEndless catches it, sets stopReason="review error on pass 3: <msg>", and returns after only 2 productive passes. The handler calls reporter.done({ ok: true, stopReason }); progress.json is written with done:true, ok:true, phase:"done". `council watch` renders the run as a successful/green completion, so the operator believes the endless review finished cleanly with full coverage when in fact it aborted early on an error. Same masking occurs on the --supervise path when it returns a terminal stop (max-attempts / wall-clock / permanent-quota outage).
- **Verifier:** Confirmed real telemetry-accuracy defect. council-companion.mjs:2570 hardcodes reporter.done({ ok: true, stopReason: out.stopReason }) unconditionally, while the two sibling handlers in the same slice derive ok from the outcome (plan @2976: ok: Boolean(out?.planSpec); build @3525: ok: out?.ok === true). Verified the error path is genuinely reached: audit-endless.mjs:130-136 CATCHES a thrown review pass, sets stopReason="review error on pass N: ...", breaks, and RETURNS NORMALLY (line 162) — it never throws — so the handler's ok:true fires with a real error stopReason. On --supervise, supervisor.mjs returns terminal supervisorStop values ("max attempts reached" @107, "wall-clock cap reached" @95, the permanent-quota terminal @93, "aborted (anomaly)" @78), still hardcoded ok:true. progress.mjs:162-168 folds ok:true into next.phase = ok===false ? "failed":"done" → persists phase:"done", ok:true. watch.mjs:505/524 computes failed = (s.ok===false || phase==="failed") → status:"done" with RUN_EMOJI.done="🟢", so a run aborted on a review error or supervisor terminal stop shows a green "done" badge on the live dashboard. Refutations fail: the stop-reason detail line (watch.mjs:599/705) is still printed, softening but not curing the defect — the status badge/color/phase/ok (the at-a-glance signal) are wrong; and "endless is always ok by design" is refuted by the two siblings that deliberately derive ok and by the reducer explicitly mapping ok===false→"failed". Clean stops (max passes/budget/diminishing returns) are cleanly distinguishable from error/terminal stops, so a correct ok:false is computable. Impact is confined to the progress.json/watch dashboard channel — JSON output and console report carry the true stopReason, and endless does not gate its exit code on this — so P2 (dashboard telemetry accuracy on the failure path the dashboard exists to surface), matching the claim. Note the code paths referenced by exact file:line all check out; the only minor correction to the claim is that watch DOES still print the stop line, so masking is of the status/color, not total suppression.

## 25. [P2] Plain `review`/`adversarial` silently drops the Codex seat when only the standalone codex CLI is installed (no companion), while setup reports READY

- **File:** `plugins/council/scripts/council-companion.mjs:432` · **Category:** facade
- **Detail:** `runCodexReview` (the plain review/adversarial path) hard-requires the codex COMPANION: `if (!backends.codex.companionAvailable) return { agent:"codex", skipped:true, reason:"codex companion not found" }`. It has NO codex-CLI-direct fallback. This is the outlier: (a) `runGrokReview` in the same file DOES fall back to the grok CLI (lines 475-517), and (b) `runCodexStructured` in lib/agents.mjs (used by deliberate/solve/audit) falls back to `runCodexCli` when the companion is absent but the CLI is available (agents.mjs:344-346, with the explicit comment "the standalone CLI is a first-class path, not a degraded one"). Meanwhile `handleSetup` computes `codexReachable = !wants("codex") || backends.codex.companionAvailable || backends.codex.cli.available` (line 218) and suppresses the install hint unless BOTH are missing (line 263). `probeBackends` derives `companionAvailable` (findCodexCompanion) and `cli.available` (binaryAvailable("codex")) independently, so the standalone-codex-CLI-without-companion state is genuinely reachable. Net effect: the codex CLI-direct code path is unreachable from the plain review mode, and setup advertises a seat the review path never reaches (the anti-facade class this codebase already fought).
- **Failure scenario:** Environment: standalone `codex` CLI on PATH and logged in, but NO codex companion installed; grok available. `council setup` prints READY and lists codex as reachable (no install hint). `council review` then returns codex = {skipped, reason:"codex companion not found"}, so the 'Codex + Grok' review silently runs Grok-only. `council deliberate` in the IDENTICAL environment uses codex (runCodexStructured -> runCodexCli). Setup, review, and deliberate thus disagree about whether Codex participates.
- **Verifier:** CONFIRMED real. In the environment "standalone `codex` CLI on PATH + logged in, no codex-companion, grok available", three paths disagree about whether Codex participates:

1. `runCodexReview` (council-companion.mjs:432-434) — the plain `review`/`adversarial` runner — returns {skipped, reason:"codex companion not found"} when !companionAvailable and has NO cli.available fallback. It is dispatched unconditionally at line 793; the `review`/`adversarial` subcommands reach it via handleReview(rest,false,false)/(rest,true,false) at council-companion.mjs:3549-3554. Grok still runs so allSkipped=false → exit 0: the review completes Grok-only (docs review.md:17,76 promise "Codex + Grok in parallel").

2. `runGrokReview` in the SAME file (lines 475-517) DOES fall back to the grok CLI, and `runCodexStructured` (agents.mjs:343-346, used by deliberate/solve/audit) DOES fall back to runCodexCli ("the standalone CLI is a first-class path, not a degraded one"). The deliberate R1 loop dispatches codex via runCodexStructured (deliberate.mjs:277) and does NOT pre-filter built-in seats through seatActive (line 303 filters only OpenRouter seats) — so codex participates in the identical environment.

3. handleSetup computes codexReachable = ... || backends.codex.cli.available (council-companion.mjs:218) and suppresses the install hint unless BOTH are missing (line 263) → prints READY and lists codex reachable.

Reachability confirmed: probeBackends (discover.mjs:133-153) derives companionAvailable from findCodexCompanion() (searches for the openai-codex plugin file) and cli from binaryAvailable("codex") independently, so the CLI-only/no-companion state is genuinely reachable — and is exactly where the setup hint's own first option (`npm i -g @openai/codex`) lands a user. Net: setup advertises a Codex seat the quick-review/adversarial mode never fills, while deliberate does fill it — the anti-facade "built + gated + unreachable-from-this-path" class the codebase explicitly fights. No upstream gate refutes it. P2: bounded to the optional --quick/--adversarial modes, the skip is at least printed in the report, no data loss/security impact; fix is to give runCodexReview the same runCodexCli fallback the other two paths already have.

## 26. [P2] `setup --init --reviewers <typo>` silently mis-scaffolds the reviewer list with no warning

- **File:** `plugins/council/scripts/council-companion.mjs:337` · **Category:** input-validation
- **Detail:** `scaffoldPolicyFile` passes `options.reviewers` straight into `normalizeReviewers` (line 337), which silently drops any token that is not claude/codex/grok and, when the result is empty, falls back to all three (policy.mjs:276-280). No warning is emitted on the --init path. This is inconsistent with `handleReview`, which explicitly warns on unknown `--reviewers` tokens (lines 900-903) precisely because 'running MORE reviewers than intended is the opposite of the user's intent'. The scaffold is the durable config, so the mistake persists for every later run.
- **Failure scenario:** `council setup --init --reviewers grok,claud` (a typo of 'claude') writes `reviewers: [grok]` to .council.yml, silently dropping the intended Claude seat with no warning. `council setup --init --reviewers gork` writes `reviewers: [claude, codex, grok]` (all three) although the user asked for one seat -- the opposite of intent -- again with no warning.
- **Verifier:** Confirmed. In council-companion.mjs, the `--init` branch of handleSetup (lines 192-201) calls scaffoldPolicyFile, which at line 337 passes options.reviewers straight into normalizeReviewers and never calls unknownReviewers. normalizeReviewers (policy.mjs:276-280) silently drops non-claude/codex/grok tokens and, when the result is empty, falls back to all three. So `setup --init --reviewers grok,claud` writes `reviewers: [grok]` (Claude seat dropped) and `setup --init --reviewers gork` writes `reviewers: [claude, codex, grok]` (all three, the opposite of a one-seat request) — both with no warning. This is inconsistent with the two sibling paths that DO warn: handleReview (900-903) and the non-init setup path (210-213), whose own comments (policy.mjs:283-285, companion:898-899) explicitly state the warning exists precisely to stop typos from silently expanding the reviewer set. The scaffold writes the durable .council.yml, so the misconfig persists for every later run. Mitigating detail (why P2, not higher): line 197 echoes the full written contents to the terminal, so the resulting reviewers line is visible — it is not fully invisible, and the output is valid YAML — but no explicit dropped-token warning is emitted, and the `gork`->all-three case reads as a normal default. Real, low-severity consistency/UX defect with a concrete wrong-outcome scenario; not a facade or correctness/security issue.

## 27. [P2] DANGEROUS_CODE denylist matches tokens inside string/expected-value literals, false-rejecting safe char-tests (disables the gate for security modules)

- **File:** `plugins/council/scripts/lib/chartest-gate.mjs:80` · **Category:** correctness-false-reject
- **Detail:** parseCharTest runs DANGEROUS_CODE (line 80) against the raw extracted test body without stripping strings or comments (check at line 100). The regex matches substrings like `node:fs`, `child_process`, `process.binding`, `eval(`, `import(` ANYWHERE in the code, including inside string literals and the expected-value literals the generator is REQUIRED to capture. So a perfectly safe characterization test whose observed output or arguments contain one of those tokens is classified as unsafe and parseCharTest returns null, forcing acceptCharTestForTarget to return accepted:false -> propose-only. This structurally prevents the gate from ever guarding a refactor of any target whose observable behaviour involves those strings -- which includes this repo's own security-relevant modules (chartest-gate.mjs itself, chartest-node-harness.mjs, import-graph.mjs), i.e. it cannot dogfood/guard the very modules where a behaviour-preserving refactor is most sensitive. Fail-safe (never accepts a bad refactor) but a real defect in the accept decision.
- **Failure scenario:** Target import-graph.mjs exports a pure `validateSpec(x)` returning strings like "denied: node:fs". The firewalled generator writes a valid deterministic test: `import { validateSpec } from "./import-graph.mjs"; ... const r = validateSpec("node:fs"); console.log(JSON.stringify(r)); assert.equal(r, "denied: node:fs");`. DANGEROUS_CODE matches the literal `node:fs` -> parseCharTest returns null -> acceptCharTestForTarget returns {accepted:false, reason:'... not a discriminating node:test'} -> the refactor is downgraded to propose-only even though the test is safe and discriminating.
- **Verifier:** CONFIRMED. DANGEROUS_CODE (line 80) runs at line 100 on the raw, unstripped test body; its tokens (\bnode:(fs|net|...)\b, \bimport\s*\(, child_process, \beval\s*\(, process.binding) match inside string/expected-value literals, not just real code. The path is fully wired (chartest-gate → chartest-wiring makeCharTestGate → council-companion resolveCharTestGate → audit-fix.mjs:667-770 charTestGate.accept/verify around the real apply), so a null from parseCharTest → acceptCharTestForTarget {accepted:false} (line 136) → refactor downgraded to propose-only. The claim's specific example (import-graph.mjs validateSpec) is hypothetical, but the module's REAL export parseModule(rawText) is an import parser: any discriminating characterization test must feed it import-statement strings as data — e.g. parseModule('const a = import(\"child_process\"); import fs from \"node:fs\";') — which I verified is rejected (DANGEROUS_CODE matches import( and node:fs; the ALLOWED_IMPORT specifier check at 102-105 is also string-blind and independently rejects it). So a parser/linter/security module literally cannot be characterization-gated. This is a genuine, reproducible false-positive in a static denylist operating on unparsed source, systematically and silently (undisclosed, unlike the target-too-large case at line 31) preventing the §5 gate from ever upgrading a behaviour-preserving refactor of exactly the class of modules where such a guard matters most, including this repo's own infra. It is fail-safe (propose-only, never a bad auto-apply), so P2, not P0/P1 — but it is a real correctness defect in the accept decision, not a style nit.

## 28. [P2] COMPARE_ASSERT requires the `assert.<method>` prefix, rejecting valid destructured/bare node:assert assertions the prompt permits

- **File:** `plugins/council/scripts/lib/chartest-gate.mjs:83` · **Category:** correctness-false-reject
- **Detail:** COMPARE_ASSERT (line 83) only matches the property-call form `assert.equal|strictEqual|deepEqual|...`. The generator prompt (buildCharTestPrompt) tells the model to 'Use ONLY the node:test + node:assert built-ins' and to assert equality, but never mandates the `assert.` prefix. The idiomatic modern node style is `import { strictEqual, deepStrictEqual } from "node:assert/strict"; strictEqual(actual, expected);` -- a bare call with no `assert.` prefix. parseCharTest's check at line 107 fails to find COMPARE_ASSERT and returns null, so a fully valid, discriminating characterization test is classified as 'not discriminating' and the refactor is forced to propose-only. Fail-safe but it rejects a common, prompt-permitted assertion idiom, reducing the gate's auto-apply coverage.
- **Failure scenario:** Generator returns: ```js\nimport { test } from "node:test";\nimport { deepStrictEqual } from "node:assert/strict";\nimport { fmt } from "./target.mjs";\ntest("pins fmt", () => { const r = fmt(5); console.log(JSON.stringify(r)); deepStrictEqual(r, {a:"5.00"}); });\n```. ALLOWED_IMPORT/DANGEROUS_CODE pass, hasTest passes, but COMPARE_ASSERT (which needs `assert.`) does not match `deepStrictEqual(...)`, so parseCharTest returns null and the eligible refactor is rejected to propose-only despite a real, discriminating assertion.
- **Verifier:** CONFIRMED (P2, fail-safe). COMPARE_ASSERT (chartest-gate.mjs:83) only matches the property-call form `assert.<method>`, but the surrounding code explicitly permits the bare/destructured form. ALLOWED_IMPORT (line 78) whitelists `node:assert/strict` — the specifier whose idiomatic use is `import { strictEqual, deepStrictEqual } from "node:assert/strict"; strictEqual(a,b)` with NO `assert.` prefix. Tracing the claimed test through parseCharTest: hasTest passes (node:test), DANGEROUS_CODE passes (static `import {` never matches `\bimport\s*\(`; node:test/node:assert not in the module denylist), ALLOWED_IMPORT passes (node:test, node:assert/strict, ./target.mjs all allowed), but COMPARE_ASSERT does NOT match `deepStrictEqual(...)` (the only "assert" token is inside `node:assert/strict`, followed by `/` not `.`). So parseCharTest returns null at line 107 and the refactor is forced to propose-only despite a real, discriminating assertion. The prompt (buildCharTestPrompt) never mandates the `assert.` prefix. The finding is actually understated: parseCharTest is the shared firewall for build-step.mjs too (line 458), whose prompt (line 287) explicitly permits `node:assert/strict`, nudges toward namespace/named imports (line 288), and lists the accepted assertions as `(assert.equal / strictEqual / deepStrictEqual / match …)` — showing `strictEqual`/`deepStrictEqual` BARE. A model following that example literally is rejected by the firewall, spuriously failing a whole valid build step. Internal inconsistency between the regex, the import whitelist, and both generator prompts. Fail-closed (spurious rejection, never wrong auto-apply), so P2 as claimed.

## 29. [P2] verifiedCount = targets.length double-counts refuted (and never-verified) findings as "held up"

- **File:** `plugins/council/scripts/lib/verify.mjs:153` · **Category:** correctness
- **Detail:** verifyFindings() calls partitionByRefutation(merged, refutedKeys, targets.length, ...) (verify.mjs:153), so the returned verifiedCount is the number of findings ELIGIBLE for verification, not the number that actually SURVIVED the refutation. That value is surfaced as the "held up" count: deliberate.mjs:508 maps it to verification.verifiedCount -> metrics.mjs:59/158 review.verified -> the dashboard line watch.mjs:344 ("{verified} held up · {refuted} refuted") and the metric metrics.mjs:204 holdup% = 100*verified/(verified+refuted). Because verifiedCount counts every eligible finding, it INCLUDES the ones that were refuted (also counted in refutedCount) AND the ones that were never actually checked (verifierFor returned null / budget-starved / untrustworthy result -> verdict null). "held up" and "refuted" are therefore not disjoint and their sum exceeds the number of findings checked. The correct held-up count is at most targets.length - refuted.length (ideally the count of trustworthy non-refuted verdicts).
- **Failure scenario:** Deliberate run with 2 P1 single-agent findings; the verifier refutes 1 and supports 1. targets.length=2 -> verifiedCount=2, refutedCount=1. The dashboard/metrics report "2 held up · 1 refuted" (3 outcomes from 2 findings) and hold-up = 2/(2+1) = 67%, when reality is "1 held up · 1 refuted" = 50%. The refuted finding is double-counted in the held-up total; adding a third eligible finding that has no independent verifier (verdict null) inflates "held up" to 3 while it was never actually checked.
- **Verifier:** Confirmed real. verify.mjs:153 passes `targets.length` as verifiedCount, and partitionByRefutation returns it verbatim (line 188). `targets` (line 113) is every finding ELIGIBLE for verification; `refuted` (line 172) is a strict subset of those targets, and never-checked findings (verifierFor null at line 116, budget-starved at line 121, or untrustworthy verdict null at line 138) stay in the count too. But downstream this value is presented as the disjoint "held up" outcome: deliberate.mjs:508 → metrics.mjs:59/158 (verify.verified) → watch.mjs:344 ("{verified} held up · {refuted} refuted") and metrics.mjs:204 (hold-up% = 100*verified/(verified+refuted)). Because verified includes the refuted ones, held-up and refuted overlap and their sum exceeds the number checked. Concrete: 1 single-agent P1 target that the verifier refutes → verifiedCount=1, refutedCount=1, so the dashboard shows "1 held up · 1 refuted" (2 outcomes from 1 finding) and hold-up=100*1/(1+1)=50%, when reality is 0 held up / 0% hold-up. If EVERY target is refuted the formula still reports 50% instead of 0%. Adding a target with no independent verifier (verdict null) inflates "held up" further without any check. Correct held-up count is at most targets.length - refuted.length. This is a metrics/dashboard accuracy bug only — it never changes a command outcome or exit code and the actual finding partition is correct — hence P2, not P1.

## 30. [P2] A verifier "support" verdict with NO evidence still over-promotes the finding to the strongest-supported state (asymmetric with demotion)

- **File:** `plugins/council/scripts/lib/verify.mjs:146` · **Category:** correctness
- **Detail:** The refutation is only trusted to DEMOTE when it was evidence-based: refutedKeys sets demotable = r.hadEvidence (verify.mjs:146-151) and partitionByRefutation requires demotable !== false to move a finding to the refuted bucket, per the explicit rule "default-refute with no evidence is unreliable". But the SUPPORT direction has no such gate: when the verifier returns {refuted:false} the same annotation verified:{refuted:false, demotable:false} is written even if hadEvidence was false (buildEvidence returned "(no file evidence available)"). Downstream isVerifiedSupported() (audit-normalize.mjs:60-61) reads Boolean(verified) && !isRefuted — it does NOT consult demotable/hadEvidence — so the finding is promoted to evidenceState "adversarial-verified": confidence floor ~0.85 (deriveConfidence), severity cap lifted (cappedSeverity verified:true), lifecycle "confirmed". A no-evidence run can thus only PROMOTE, never demote — the mirror of the exact inversion class the council fixed (a finding the verifier could not examine ranks among the strongest). Symmetry requires gating support on hadEvidence too (e.g. only annotate support when hadEvidence, or have isVerifiedSupported require demotable).
- **Failure scenario:** On the autonomous audit path, a codex-only P1 finding whose file cannot be read (moved/renamed since review, or f.file empty) -> buildEvidence yields "(no file evidence available)", hadEvidence=false. shouldVerify still selects it (it only checks severity+consensus). Grok is spawned with an empty-evidence prompt and, unable to refute the plausible finding text, returns {refuted:false}. verify.mjs annotates verified:{refuted:false,demotable:false}; toCanonicalFinding then reads isVerifiedSupported=true -> adversarial-verified, confidence 0.85, severity cap lifted, lifecycle confirmed. The finding ranks as high-confidence/confirmed and can clear a confidence-floor gate, despite the verifier having no code to examine.
- **Verifier:** CONFIRMED real. The support (refuted:false) direction has no evidence gate, mirroring only half the fix the council already made for the refuted direction. Chain verified end-to-end in the actual code:

1. verify.mjs:124-125 calls buildEvidence(repoRoot, [one finding], "") with an EMPTY fallback. deliberate.mjs:109-111 returns the sentinel "(no file evidence available)" whenever the finding's file is unreadable/missing/outside-repo or f.file is falsy → hadEvidence=false. This is reachable: a cross-cutting P1 with empty f.file, or a file moved/renamed between review and verify.
2. shouldVerify (verify.mjs:60-66) gates ONLY on severity + !consensus — no evidence check — so the no-evidence finding is still selected and a verifier is spawned.
3. verify.mjs:142-152 writes the annotation for EVERY verdict as {refuted, demotable: r.hadEvidence}. For a support verdict with no evidence: {refuted:false, demotable:false}.
4. partitionByRefutation (verify.mjs:171) consults demotable ONLY inside isRefuted (the demote branch); the support branch just keeps the annotated finding (scoped.all), wired downstream via audit-grouped-review.mjs:213/307.
5. isVerifiedSupported (audit-normalize.mjs:60-61) reads Boolean(verified) && !isRefuted and NEVER consults demotable/hadEvidence (grep confirms demotable is read at exactly one site, verify.mjs:171). So the no-evidence support finding yields isVerifiedSupported=true.
6. Result (audit-normalize.mjs:69,83,101 + audit-risk.mjs:13): evidenceState "adversarial-verified" → confidence FLOOR 0.85, severity cap LIFTED (verified:true), lifecycle "confirmed" (STATE_ORDER 0 → ranks first). This is the exact inversion class the codebase's own comments (audit-normalize.mjs:52-53, audit-risk.mjs:11) say they fixed — but only on the refute side.

The design's stated rationale for the refute-side gate — "default-refute with no evidence is unreliable" (verify.mjs:144-145) — applies identically to a no-evidence support verdict, yet no symmetric gate exists. The one behavioral contingency (verifier returning refuted:false without evidence, contrary to r2-verify.md:3,22 which says default refuted:true) is exactly the unreliability the refute-side gate already assumes the verifier exhibits; LLM sycophancy toward a detailed, plausible finding it cannot examine makes it realistic. P2 is right: trigger is a compounded/uncommon condition and the impact is over-promotion of one finding's ranking/confidence/lifecycle (feeds fix prioritization and confidence-floor gates), not a §6 patch-gate bypass or data loss. Fix: gate the support annotation on hadEvidence (or have isVerifiedSupported require demotable !== false).

## 31. [P2] require_consensus_for is passed through un-normalized; a scalar YAML value crashes deliberate with TypeError

- **File:** `plugins/council/scripts/lib/policy.mjs:217` · **Category:** robustness
- **Detail:** mergeOptionsWithPolicy sets `requireConsensusFor: options.requireConsensusFor ?? policy.require_consensus_for ?? []` with NO array coercion (policy.mjs:217). Sibling list fields that feed `.map`/`.includes` consumers are deliberately normalized against exactly this footgun — reviewers via normalizeReviewers, peer_critique_severities via normalizeSeverityList, and skip_seats via normalizeStringList (whose comment at lines 247-248 spells out the scalar-string-vs-array hazard) — but require_consensus_for was left raw. parseSimpleYaml turns a single-value entry `require_consensus_for: security` into the string "security" (coerceScalar, policy.mjs:119), and the consumer applyConsensusPolicy (findings.mjs:571) runs `categories.map(...)` BEFORE its `if (!required.size)` guard, so a string argument throws `TypeError: categories.map is not a function`. deliberate.mjs:498 calls it unconditionally on merged options.
- **Failure scenario:** A repo ships `.council.yml` containing `require_consensus_for: security` (a scalar, not a `- list`). loadPolicy -> parseSimpleYaml stores the string "security"; mergeOptionsWithPolicy leaves requireConsensusFor as "security". `council deliberate` reaches applyConsensusPolicy(merged, "security") -> "security".map(...) -> uncaught TypeError, crashing the command. A number (`require_consensus_for: 2`) crashes identically.
- **Verifier:** Confirmed real. mergeOptionsWithPolicy (policy.mjs:217) sets requireConsensusFor without array coercion, unlike every sibling list field (reviewers/normalizeReviewers:189, peerCritiqueSeverities/normalizeSeverityList:219, skipSeats/normalizeStringList:242 whose 247-248 comment documents this exact scalar-vs-array hazard). parseSimpleYaml handles `require_consensus_for: security` via coerceScalar (policy.mjs:119) → the string "security"; a number like `require_consensus_for: 2` → 2. executeCouncilReview builds mergedOpts (council-companion.mjs:601) and calls runDeliberation with {...mergedOpts} (727); runDeliberation does not re-normalize (deliberate.mjs:242+). At deliberate.mjs:498 applyConsensusPolicy(merged, "security") is called unconditionally, and findings.mjs:571 runs categories.map(...) BEFORE the `if (!required.size)` guard on line 572, so "security".map / (2).map throws TypeError: categories.map is not a function, crashing `council deliberate`. Concrete repro: ship .council.yml with scalar `require_consensus_for: security` and run council deliberate. Claim's file path lib/deliberate.mjs (not deliberate.mjs) is slightly off but line 498 is exact. P2 is appropriate: config-triggered crash of a real feature, needs a plausible non-list config mistake, no data-loss/security impact.

## 32. [P2] skip_paths is passed through un-normalized; a scalar YAML value crashes audit/deliberate context collection

- **File:** `plugins/council/scripts/lib/policy.mjs:218` · **Category:** robustness
- **Detail:** mergeOptionsWithPolicy sets `skipPaths: options.skipPaths ?? policy.skip_paths ?? []` with no array coercion (policy.mjs:218), the same omission as require_consensus_for. A scalar policy entry `skip_paths: node_modules` becomes the string "node_modules" via coerceScalar. The consumer normalizeSkipPaths (git-context.mjs:110) does `skipPaths.map(...)`, which throws `TypeError: skipPaths.map is not a function` on a string. Since git-context is used for both the review-context collection in deliberate (deliberate.mjs:253) and the diff/untracked collection in audit, a scalar skip_paths crashes those commands. Because the policy file is read from the audited (untrusted) repo, a hostile or typo'd `.council.yml` denies service on that repo.
- **Failure scenario:** A repo ships `.council.yml` with `skip_paths: dist` (scalar). merged.skipPaths becomes the string "dist". `council audit`/`council deliberate` calls collectReviewContext -> normalizeSkipPaths("dist") -> "dist".map(...) -> uncaught TypeError, aborting the run.
- **Verifier:** Verified real. policy.mjs:218 sets skipPaths = options.skipPaths ?? policy.skip_paths ?? [] with no array coercion (unlike normalizeStringList/normalizeSeverityList siblings). A scalar `.council.yml` entry `skip_paths: dist` is parsed by parseSimpleYaml's scalar fall-through (policy.mjs:119) into coerceScalar("dist") = the string "dist" (list forms would yield arrays). loadPolicy spreads it unmodified, so merged.skipPaths === "dist". council-companion.mjs:727-728 spreads ...mergedOpts into runDeliberation, so deliberate.mjs:253 calls collectReviewContext(cwd, target, { skipPaths: "dist" ?? [] }) = "dist". Inside git-context.mjs, collectReviewContext:201 keeps skipPaths="dist", then pathspecArgs("dist") (:212) → normalizeSkipPaths("dist") (:114) → "dist".map(...) (:110) throws an uncaught TypeError: skipPaths.map is not a function, aborting the deliberate run. Same crash on the branch and untracked-collection code paths. The policy file is read from the audited (untrusted) repo, so a hostile or typo'd scalar skip_paths is a denial of service on that repo. Confirmed reachable (not a facade). P2 as claimed.

## 33. [P2] Grok override-rejection retry matches on the model's own stdout, so a completed review can be discarded and silently re-run with the default (lower) effort

- **File:** `plugins/council/scripts/lib/agents.mjs:278` · **Category:** correctness
- **Detail:** runGrokStructured decides whether a nonzero exit was caused by a rejected model/effort override by testing `/invalid params|unknown model/i` against BOTH stderr AND stdout (agents.mjs:278). stdout on a nonzero-but-nonempty grok exit (e.g. hitting `--max-turns 40`) contains grok's own review text over an UNTRUSTED diff. 'invalid params' / 'unknown model' are common literals in real source under review (validation code, error strings). When such content is present and grok exits nonzero, the guard misfires: `hasOverrides` is effectively always true (grokEffort defaults to 'high', line 263-264), so the code throws away the already-produced review and re-runs the entire task with baseArgs (no --model/--effort), silently downgrading from the user's pinned 'high' effort to the CLI default and mislabeling the result '(CLI default after rejected override)' (lines 294-299). The match should be restricted to stderr (the channel where grok emits arg-validation errors), not the model-generated stdout.
- **Failure scenario:** grok reviews a file whose text includes the string 'unknown model' and exits nonzero after exhausting --max-turns while having emitted a full/partial review to stdout. The retry guard matches on that stdout, discards the completed high-effort review, and re-runs from scratch with the CLI's default model/effort — 2x cost and a silently lower-effort review attributed to a 'rejected override' that never happened.
- **Verifier:** Confirmed. runGrokStructured's override-rejection retry (agents.mjs:274-282) tests /invalid params|unknown model/i against `${result.stderr}\n${result.stdout}`, but stdout carries grok's own model-generated review text (plain mode streams assistant text; json mode wraps it in {text,...}) over an untrusted diff. All three sibling guards are satisfiable simultaneously: (1) hasOverrides is unconditionally true because grokEffort defaults to the truthy "high" (lines 263-264); (2) status!==0 holds on any grok self-terminated nonzero exit, e.g. exhausting --max-turns 40; (3) !timedOut holds because process.mjs sets timedOut ONLY on our own options.timeoutMs kill (process.mjs:203,260-266), never on grok's internal max-turns. When such a nonzero exit occurs after grok has streamed review text containing the trap phrase, the guard misfires and re-runs with baseArgs, which omits --effort/--model (they were pushed onto `args`, not `baseArgs`, lines 265-266) — silently downgrading from pinned "high" to CLI default, doubling cost, discarding the produced review, and mislabeling provenance as "(model/effort overrides rejected, CLI defaults used)" though no rejection happened (lines 294,299). The trigger is concrete and self-referential: agents.mjs is the only plugin file containing the literals "invalid params"/"unknown model", so grok reviewing this file (or any validation/error-handling code, common) will discuss those tokens in stdout. The function is on real review/§6-seat paths (seats.mjs, audit-patch-reviewer.mjs, build-step-reviewer.mjs, deliberate.mjs, verify.mjs), not a facade. Severity P2, not higher: the seat still votes so the §6 fail-closed gate is not bypassed, and there is no data loss or security exposure — the harm is a wasted lower-effort re-run, a discarded review, and corrupted labeling. Correct fix: restrict the match to stderr, the channel where grok emits arg-validation errors. The one soft assumption (grok emits stdout on a nonzero exit) is satisfied by streaming partial output before any mid-run nonzero exit, so the vehicle is broader than max-turns alone.

## 34. [P2] Chunked response body decoded per-Buffer without setEncoding — multibyte UTF-8 corrupted at chunk boundaries

- **File:** `plugins/council/scripts/lib/openrouter-agent.mjs:212` · **Category:** data-corruption
- **Detail:** In postJson the response has no res.setEncoding("utf8") and no StringDecoder; each 'data' event delivers a raw Buffer and line 212 `body += c` coerces every chunk to a string independently. A UTF-8 character whose bytes straddle two 'data' events (2-byte accented letters, 3-byte CJK, 4-byte emoji, or unicode identifiers/string literals inside a code snippet) is split across two independent Buffer→string decodes and each half becomes a U+FFFD replacement character. res.body therefore contains corrupted characters before JSON.parse at line 342. JSON.parse still succeeds (U+FFFD is valid inside a JSON string value), so the corruption silently propagates into the seat's stdout (`content`) and into the downstream finder/§6 parse. §6 verdict keywords are ASCII so unanimity is unaffected, but any non-ASCII content a seat reports (finding descriptions, quoted code with unicode) is silently garbled. Fix: call res.setEncoding("utf8") (or accumulate Buffers and Buffer.concat before decoding) so chunk boundaries are handled by StringDecoder. Note the RESPONSE_CAP slice at line 216 also becomes byte-accurate once encoding is set consistently.
- **Failure scenario:** An OpenRouter seat returns a large response (any TCP-fragmented body, common past ~16-64KB) whose message content contains a non-ASCII character — e.g. a finding quoting `const café = ...` or an emoji — and that character's bytes land on a 'data' chunk boundary. `body += c` decodes each half separately, inserting U+FFFD; the seat's stdout/finding text is delivered with a corrupted character (café→caf��) instead of the model's actual bytes.
- **Verifier:** Confirmed by reading plugins/council/scripts/lib/openrouter-agent.mjs:204-221. The response handler has no res.setEncoding("utf8"), no StringDecoder, and no Buffer.concat (grep confirms all three absent). res.on("data", (c) => ...) therefore delivers raw Buffers, and `body += c` at line 212 coerces each Buffer independently via toString('utf8') before concatenation. HTTP 'data' events emit socket bytes at arbitrary (non-character-aligned) offsets; any response over ~1400B (TCP MSS) — routine for whole-file review output — is split across multiple chunks, so a 2/3/4-byte UTF-8 char straddling a boundary is decoded in two halves, each becoming U+FFFD. JSON.parse at line 342 succeeds (U+FFFD is valid inside a JSON string), so the corrupted text flows through extractMessageText into the seat's stdout (line 355) and downstream finders. postJson is the live default transport (line 299), not a facade. Concrete failure: a seat returns a >1400B review whose content quotes `const café = ...` or an emoji, TCP fragmentation lands that multibyte char on a chunk boundary, and the surfaced finding shows caf��. Correctly scoped as P2: §6 verdict keywords are ASCII so the fail-closed unanimity gate and exit codes are unaffected; only non-ASCII content is silently garbled. I attempted to refute via the surrounding guards (deadline, cap, param-fallback) but none introduce setEncoding and none prevent the per-chunk decode. Real.

## 35. [P2] Claude finder has no graceful fallback on a rejected --model/--effort override, unlike the grok seat — a stale model pin silently drops the Claude eye every run

- **File:** `plugins/council/scripts/lib/claude-agent.mjs:95` · **Category:** silent-degrade
- **Detail:** runClaudeStructured (line 92-99) pushes --model options.claudeModel (line 74) and --effort options.claudeEffort (buildClaudeArgs line 71-72) and then runs the CLI with no recovery if those override ids are rejected. The sibling grok seat explicitly guards this exact case: agents.mjs:274-282 detects /invalid params|unknown model/i on a nonzero, non-timeout exit and RE-RUNS with the CLI's own defaults ('Rather than failing the whole round, retry once with the CLI's own defaults'). The Claude finder does not. When claude exits nonzero because of a rejected --model, the result has status!=0 and parseMissed===false (parseMissed only flips true for a status-0 run whose parse fails). In the finder's consumer (audit-review.mjs reviewUnit, lines 245-256) such a result is neither added to docs nor counted in `unparsed` — it vanishes — yet coverage.reviewers.claude is still reported true via reviewerActive (audit-review.mjs:456). So the Claude eye contributes nothing on every unit while the report claims it was an active reviewer: exactly the 'a configured model is silently dropped' class the codebase states it purged elsewhere (see the same comment in agents.mjs runCodexStructured header). claudeEffort has the same exposure if the CLI hard-errors on an unknown value (the §6 comment only claims --effort 'warns + falls back', which is unverified for --model).
- **Failure scenario:** Policy/config pins claudeModel to a now-deprecated id (e.g. an old 'claude-opus-4-8' that the installed CLI no longer accepts). audit review probes Claude available:true, dispatches runClaudeStructured; the CLI exits nonzero ('unknown model'). reviewUnit records status!=0, parseMissed false -> Claude adds zero findings and is NOT counted in unparsedReturns, but coverage.reviewers.claude=true. Result: six-eyes silently becomes five-eyes on every unit with no signal, while grok in the identical situation self-heals by retrying with CLI defaults.
- **Verifier:** CONFIRMED by reading the code. The claim's every load-bearing link holds: (1) runClaudeStructured (claude-agent.mjs:78-104) pushes --effort (71-72) and --model claudeModel (74) then runs the CLI (95) with no recovery; (2) grok's runGrokStructured (agents.mjs:268-282) explicitly guards this exact case, retrying once with baseArgs on a nonzero, non-timeout exit matching /invalid params|unknown model/i, and its retried result returns status===0 so it survives the consumer; (3) on a nonzero claude exit, runStructuredWithRetry (agents.mjs:85-112) has okRun require status===0, so the loop breaks immediately at line 91 and parseMissed stays false, with no `skipped` field set; (4) in reviewUnit (audit-review.mjs:245-256) such a result matches neither `r.status===0` nor `r.parseMissed`, and `r.skipped` is falsy, so it is neither added to docs nor counted in `unparsed` — it vanishes (identical drop in globalReduce 308-319); (5) coverage.reviewers.claude (audit-review.mjs:456 -> reviewerActive -> seatActive, seats.mjs:33) is `!skipClaude && Boolean(backends.claude.cli.available)`, depending only on the probe, never on run outcome, so it reports true. Net: when a policy/CLI-pinned claudeModel is a stale/deprecated id the installed CLI rejects (model_not_found -> `claude -p` exits nonzero, a realistic behavior and exactly the condition grok self-heals from), the Claude eye contributes zero findings on every unit and is NOT counted in unparsedReturns, yet the coverage report claims claude was an active reviewer — silently six-eyes -> five-eyes with no signal. This is the codebase's own explicitly-purged 'a configured model is silently dropped' class (agents.mjs:318-321). The one empirical assumption (claude CLI hard-errors on a rejected --model; the repo's comments only assert --effort 'warns+falls back', never --model) is realistic and is the same case grok already defends against, so the defensive asymmetry is the defect regardless. P2 rather than higher: it degrades review coverage/report honesty but requires an invalid model pin to trigger and does not cause data loss, crash, security failure, or affect the §6 fail-closed auto-apply path (which vetoes on a missing seat independently).

## 36. [P2] recordAndAnnotate double-counts timesSeen and falsely sets seenBefore when two distinct findings collide on the coarse fingerprint in one run

- **File:** `plugins/council/scripts/lib/ledger.mjs:111` · **Category:** correctness
- **Detail:** In the annotate loop (lib/ledger.mjs:108-135) `prior = map.get(fingerprint)` reads from the SAME `map` that the loop is mutating via `map.set(fingerprint, ...)` (line 119). If two DISTINCT items in `merged.all` produce the same ledger fingerprint, the second item sees the entry the first item just wrote as its `prior`: timesSeen becomes stale+2 in one run (line 111), and the annotation returns `seenBefore: Boolean(prior)=true` / `ledgerStatus: 'open'` (line 134) for a finding that was never seen before. The ledger fingerprint (file + Math.floor(line/50) bucket + sorted first-8 significant tokens) is COARSER than mergeFindings' dedupe key (file + first-48-char title) and coarser than its fuzzy-merge (titleSimilarity>=0.4 or within-10-lines+shared-token), so two genuinely-different findings can share a fingerprint yet survive as separate entries in merged.all. audit-review.mjs:396 explicitly pre-dedupes by fingerprintFinding 'so one finding isn't counted (or ledger-incremented) twice in a single run' — proving the maintainers know this hazard — but deliberate.mjs:515 and audit-grouped-review.mjs:184 pass un-deduped merged.all. recordAndAnnotate is therefore not idempotent w.r.t. duplicate fingerprints and depends on the caller for correctness of its core seenBefore/timesSeen contract.
- **Failure scenario:** Empty ledger. A single `council deliberate` (or grouped) run yields two findings in a.mjs, lines 5 and 45 (both bucket 0, 40 apart so not lineClose), titles 'Leak cache with from into here' and 'cache leak' — stopword/short-word padding keeps titleSimilarity<0.4 so mergeFindings keeps them separate, but both reduce to the sorted significant-token set 'cache-leak' → identical fingerprint a.mjs::0::cache-leak. Item1: prior=undefined → timesSeen=1, seenBefore=false, map.set. Item2: prior=the just-set entry → timesSeen=2, seenBefore=TRUE, ledgerStatus='open'. After ONE run the persisted entry has timesSeen=2 and the second finding is reported to the user/downstream as an already-flagged recurring issue though nothing preceded it.
- **Verifier:** CONFIRMED. recordAndAnnotate (ledger.mjs:107-135) reads `prior = map.get(fingerprint)` (110) from the very Map it mutates via `map.set(fingerprint, ...)` (119) inside the same `.map()` loop, with no internal dedupe. If two DISTINCT items in merged.all share a ledger fingerprint, the second reads the first's just-written entry: timesSeen=(prior?.timesSeen??0)+1 becomes 2 (111), seenBefore=Boolean(prior)=true (134), ledgerStatus='open' — for a finding nothing preceded — and the persisted entry keeps timesSeen=2 after ONE run against an empty ledger.\n\nThe collision is realizable because fingerprintFinding (23-42) filters STOPWORDS + <4-char tokens, sorts them, and buckets the line by floor(line/50), whereas the merge dedupe key findingKey (315-317) is file::title[:48] with no stopword filter and the fuzzy gate fuzzyMatch (357-369) needs titleSimilarity>=0.4 (titleSimilarity does NOT strip stopwords) or |Δline|<=10. The claim's example holds: 'Leak cache with from into here'@5 and 'cache leak'@45 both reduce to a.mjs::0::cache-leak, yet similarity=2/6=0.33<0.4 and |5-45|=40>10, so mergeFindings keeps them separate. An even stronger, more realistic trigger: shouldMergeBuckets (371-374) requires disjointAgents, so ONE agent emitting two differently-titled near-duplicates in the same file+50-line bucket (e.g. the same issue surfaced under two lenses in the six-eyes design) yields two merged.all items with an identical fingerprint even at similarity 1.0 — same-agent buckets never fuzzy-merge.\n\nCaller asymmetry proves the hazard is unguarded: audit-review.mjs:396-402 pre-dedupes by fingerprintFinding before recordAndAnnotate (comment: 'so one finding isn't counted (or ledger-incremented) twice in a single run'), but deliberate.mjs:515 and audit-grouped-review.mjs:184 pass un-deduped input. recordAndAnnotate is therefore not idempotent and depends on the caller for its seenBefore/timesSeen contract on those two paths.\n\nImpact is bounded to the ledger's recurrence-reporting metadata: a brand-new finding is reported as an already-flagged recurring issue and timesSeen is inflated by 1. No data loss, no effect on the §6 patch gate or exit codes. P2 is correct.

## 37. [P2] Concurrent runs that both see the same finding lose a timesSeen/consensusSeen increment (max-merge over a stale base)

- **File:** `plugins/council/scripts/lib/ledger.mjs:82` · **Category:** concurrency
- **Detail:** mergeAndWriteLedger reconciles counters with `Math.max(entry.timesSeen ?? 0, other.timesSeen ?? 0)` (line 82) and the same for consensusSeen (line 83). `entry` carries this run's increment computed from the pre-lock read; `other` is the fresh on-disk value. When two council jobs concurrently see the same finding, both compute base+1 from the same stale base, and the second writer's max(base+1, base+1)=base+1 discards one sighting instead of summing to base+2. Because the module iterates the WHOLE ledger (map = full prior ledger + this run's touches) it cannot tell a touched entry from an untouched one, so it cannot safely apply the delta to the fresh disk value (other.timesSeen + 1 for touched). The lock prevents entry loss, but the counters — the 'seen N times' signal the ledger exists to provide — silently under-count under the exact concurrent-finish scenario the lock was added for.
- **Failure scenario:** Ledger entry X has timesSeen=5. Two `audit` jobs run concurrently and both find X. Both readLedger (X=5) and set entry.timesSeen=6. Job A takes the lock, writes X=6. Job B takes the lock, re-reads disk (X=6), merges Math.max(6,6)=6, writes 6. Two runs saw X but timesSeen advanced 5→6 instead of 5→7 — one sighting lost.
- **Verifier:** Confirmed real. `recordAndAnnotate` (ledger.mjs:107) reads the ledger LOCK-FREE and computes `timesSeen = prior+1` (line 111) from that stale snapshot; the lock is only taken later inside `mergeAndWriteLedger`, which reconciles with `Math.max(entry.timesSeen, other.timesSeen)` (line 82; same for consensusSeen line 83). Concrete failure: X.timesSeen=5 on disk; two concurrent jobs both flag X, both read 5 before either writes, both set entry=6. Job A locks and writes 6; Job B locks, re-reads disk=6, computes max(6,6)=6, writes 6. Counter advances 5→6 instead of 5→7 — one sighting silently lost, in exactly the concurrent-finish scenario the lock was added for (comment lines 66-67). The structural claim also holds: `map` in mergeAndWriteLedger is the full prior ledger with only this run's findings mutated, so `Math.max` is genuinely needed to protect untouched entries a concurrent job incremented, and the merge cannot distinguish a touched entry (needs other+1) from an untouched one (needs max) — hence it under-counts touched entries. Adversarial refutation attempts all failed: the read is deliberately lock-free ('best effort' comment 104-106) with no outer lock in any caller, and concurrency is a designed-for scenario per the lock's own rationale. Severity capped at P2 (not P1) because every consumer of these counters — council-companion.mjs:1393/1404, overview.mjs:39/77, html-report.mjs:50, findings.mjs:656 — uses them only as informational sort/badge/recurrence-filter signals; no patch gate, auto-apply decision, or exit code depends on the exact value, and the lock still prevents loss of the entry itself. Impact is a cosmetic under-count of the 'seen N times' recurrence signal under a rare race.

## 38. [P2] withFileLock stale-lock steal is a TOCTOU race that lets two processes into the critical section (ledger fingerprint loss)

- **File:** `plugins/council/scripts/lib/state.mjs:138` · **Category:** concurrency
- **Detail:** In withFileLock (state.mjs lines 136-144) the stale-lock steal is non-atomic: on EEXIST it does statSync(lockPath).mtime, and if older than staleMs it rmSync(lockPath,{force:true}) then `continue` to re-mkdir. Two concurrent processes can BOTH pass the staleness check on the same old lock and both rmSync. Because rmSync uses force:true it never errors even if the dir is already gone, so the sequence P1.rmSync -> P1.mkdirSync(acquire) -> P2.rmSync (deletes P1's FRESH lock) -> P2.mkdirSync(acquire) leaves BOTH holding `held=true` and both running fn() at once. This defeats the exact guarantee the lock exists for: its sole production caller, ledger.mjs mergeAndWriteLedger (lines 68-89), does a read-merge-write of the shared findings ledger specifically so 'two jobs finishing at once can't lose each other's fingerprints' (also stated in CHANGELOG). Under the race both callers read the same on-disk ledger, merge only their own map, and write -> last writer wins and the other job's fingerprints / explicit fixed|ignored status are silently lost. A correct steal needs an ownership token (write pid into the lock dir and re-verify after acquire) or an atomic rename-based claim.
- **Failure scenario:** A prior council job is SIGKILLed/timed-out while holding ledgerLockPath, leaving a lock dir >30s old (stale). Two background council jobs on the same workspace finish at nearly the same instant; both observe EEXIST, both stat the same stale lock, both rmSync and both mkdir-acquire. Both enter mergeAndWriteLedger concurrently, each reads the pre-existing ledger, and the second writeFileAtomic overwrites the first -> the first job's newly-seen fingerprints (or a just-recorded fixed/ignored status) vanish from the ledger.
- **Verifier:** Confirmed real TOCTOU concurrency bug in withFileLock (state.mjs:136-144). The stale-lock steal is non-atomic: after mkdirSync throws EEXIST, the code does statSync(mtime) then, if stale, an UNCONDITIONAL rmSync(lockPath,{force:true}) followed by continue->mkdirSync to acquire. There is no ownership token and no re-verification after acquire. Because force:true suppresses ENOENT, rmSync happily deletes whatever is at lockPath. Two processes that both hit EEXIST on the same stale dir both pass the staleness check, and the interleaving P1.rmSync(old)->P1.mkdir(acquire,enter fn)->P2.rmSync(deletes P1's FRESH lock)->P2.mkdir(acquire,enter fn) puts BOTH into the critical section at once. The atomic-mkdir guarantee is destroyed by the unconditional force-remove. Not a facade: the lock is reached via ledger.mjs mergeAndWriteLedger/resolveLedgerEntry/reconcilePendingFixes, called from audit-review, audit-fix, audit-grouped-review, and deliberate. Concrete outcome: both callers concurrently read the same on-disk ledger, merge only their own map, and last-writer-wins overwrites the other -> lost fingerprints / lost fixed|ignored status, defeating the very guarantee the lock exists for. P2 is appropriate: it requires a stale lock (crash/SIGKILL while holding, or >30s suspension of a normally-fast op) plus two jobs finishing in the tight interleaving window, and the blast radius is the cross-run dedup/memory ledger, not the reviewed code's correctness. Correct fix: pid ownership token re-verified after acquire, or an atomic rename-based claim.

## 39. [P2] quoteForCmd uses CommandLineToArgvW escaping that cmd.exe does not honor -> embedded quote + metachar escapes the shell

- **File:** `plugins/council/scripts/lib/process.mjs:29` · **Category:** security-shell-injection
- **Detail:** prepareSpawn (Windows, shell:true path for bare-name and .cmd/.bat commands) assembles the cmd.exe command line by joining quoteForCmd(token). quoteForCmd (line 29) escapes an embedded double-quote as \" — the MSVCRT / CommandLineToArgvW convention. cmd.exe's own tokenizer does NOT understand backslash-escaped quotes: it counts every literal " as a toggle of its in-quotes state. So for an arg like  x" & calc & "y , quoteForCmd emits  "x\" & calc & \"y" . cmd.exe reads the second quote as CLOSING the quoted span, leaving  & calc &  OUTSIDE quotes, where & is a command separator -> calc runs. The prepareSpawn guard (lines 41-56) explicitly fails-closed on % and on newline but has no guard for this embedded-quote case, so the file's stated 'no shell injection' invariant has a hole. Reachability is currently narrow (mitigating, hence P2 not P1): on POSIX needsCmdShell always returns false so the shell path is Windows-only, and Windows filenames cannot contain ", so path/cwd args (the common dynamic argv) cannot trigger it; untrusted model prompts are routed via stdin or --prompt-file, never argv. The live exposure is non-path argv strings that CAN carry a quote — e.g. --model/--effort/--resume session-id values (agents.mjs runGrokStructured lines 258/265/266) or any future caller of this exported helper that puts model-generated text into argv on Windows. quoteForCmd is a public export whose sole job is safe cmd quoting and it does not achieve it.
- **Failure scenario:** On Windows, grok/codex/claude/node resolve to a bare name (discover.mjs falls back to "grok"/"codex"/"claude"/"node" when no absolute path exists) so needsCmdShell()->true and the call is routed through cmd.exe. A runCommand/runCommandAsync arg that contains a double-quote followed by a shell metacharacter, e.g. resumeSessionId or grokModel = 'a" & calc & "b', is quoted by quoteForCmd as '"a\" & calc & \"b"'; cmd.exe closes the quote at the escaped ", parses ' & calc & ' as an unquoted command separator, and executes calc — arbitrary command injection instead of passing the literal argument.
- **Verifier:** CONFIRMED. quoteForCmd (process.mjs:29) escapes embedded double-quotes as \" — the MSVCRT/CommandLineToArgvW convention, which cmd.exe's tokenizer does NOT honor. cmd.exe toggles in-quote state on every literal ", so for input like `a" & calc & "b`, quoteForCmd emits `"a\" & calc & \"b"`; cmd.exe closes the quoted span at the escaped ", leaving ` & calc & ` outside quotes where & is a command separator -> calc runs. This is the BatBadBut (CVE-2024-27980) class. Verified reachability: (1) prepareSpawn routes bare-name commands through cmd.exe with shell:true on win32 (needsCmdShell returns true when the command has no slash, process.mjs:21); (2) findGrokBinary/findClaudeBinary return bare "grok"/"claude" (and codex/node) when no absolute path is found (discover.mjs:110/127), so the shell path is live; (3) prepareSpawn's fail-closed guard (lines 41-56) blocks % and newline but has NO guard for the embedded-"+metachar case, so the file's "no shell injection" invariant genuinely has a hole — even though the author's own comment acknowledges cmd.exe "offers no reliable in-quote escape"; (4) runGrokStructured pushes options.grokModel/grokEffort/String(resumeSessionId) directly into argv unsanitized (agents.mjs:258/265/266), and Node with shell:true + prebuilt command string + args:[] hands the string to cmd.exe verbatim with no additional escaping. Refutation attempts failed: no sanitizer on these argv values, no Node re-escaping on the string-command path, guard doesn't cover ". quoteForCmd is a public export whose sole documented job is safe cmd quoting and it does not achieve it. Severity P2 (not P1) is correct: current cross-trust-boundary exposure is thin (untrusted repo content flows via stdin/--prompt-file not argv; Windows path/cwd args can't contain "), but it is a real broken security primitive and a latent injection hazard for any current/future caller putting a "-bearing string into argv on Windows. The finding is accurate and does not overclaim.

## 40. [P2] runCommandAsync decodes each stdout/stderr chunk independently -> multibyte UTF-8 split across pipe boundaries corrupts to U+FFFD

- **File:** `plugins/council/scripts/lib/process.mjs:211` · **Category:** correctness-data-corruption
- **Detail:** In the async capture path, append() does `const text = chunk.toString()` per data event (line 211) with no StringDecoder and no stream.setEncoding(). stdout/stderr arrive as raw Buffers on arbitrary byte boundaries, so a multibyte UTF-8 sequence (German umlaut, em dash '—', emoji, etc.) whose bytes are delivered across two 'data' events is decoded as two separate incomplete buffers, each yielding a replacement character (U+FFFD) instead of the real code point. The byte-accounting/clip loop is unaffected (it re-measures with Buffer.byteLength) but the captured string is corrupted. This diverges from the sync runCommand path, which passes encoding:'utf8' to spawnSync and decodes the full buffer correctly. Impact is low-to-moderate: council verdict tokens are ASCII so a corrupted char elsewhere won't flip a §6 vote, but the corrupted text is persisted into review output and audit-fix/build HTML reports (mojibake), and could in principle alter a substring match on non-ASCII content. Fix: attach new StringDecoder('utf8') per stream (write on data, .end() on close) or child.stdout.setEncoding('utf8').
- **Failure scenario:** A grok/claude review that emits a non-ASCII character (e.g. '—' = bytes E2 80 94, or 'ä' = C3 A4) where the OS pipe flushes the first byte(s) in one read and the rest in the next 'data' event. append() runs chunk.toString() on each partial buffer, so instead of '—' the captured stdout contains one or two U+FFFD replacement chars; that garbled text flows into buildAgentResult and any rendered HTML report.
- **Verifier:** Confirmed real and reproduced. In runCommandAsync (process.mjs:211), append() calls chunk.toString() on every 'data' event with no setEncoding('utf8') on the streams and no StringDecoder (grep across plugins/council/scripts finds zero setEncoding/StringDecoder). Without setEncoding, each 'data' chunk is a raw Buffer decoded in isolation, so a multibyte UTF-8 sequence split across a pipe boundary corrupts to U+FFFD. Empirically verified: bytes E2 80 94 ('—') + C3 A4 ('ä') delivered as [E2 80][94 C3][A4] decode per-chunk to four U+FFFD chars, whereas the full-buffer decode yields the correct U+2014 U+E4. This diverges from the sync runCommand path (line 82) which passes encoding:'utf8' to spawnSync and decodes the whole buffer correctly. The path is genuinely reached (not a facade): runCommandAsync is the sole capture primitive for the grok (agents.mjs:269/280), codex (agents.mjs:333/362/377) and claude (claude-agent.mjs:95) review seats, and captured stdout flows into buildAgentResult (agents.mjs:167) and into persisted review output and audit-fix/build HTML reports. The byte-accounting/clip loop re-measures with Buffer.byteLength so it neither masks nor fixes the corruption. Severity P2 is appropriate: the fault is non-deterministic (depends on OS pipe flush timing) and produces mojibake in persisted/rendered non-ASCII text, but §6 verdict tokens are ASCII so it cannot flip a gate vote. Fix: attach a per-stream StringDecoder('utf8') (write on data, end() on close) or call child.stdout/stderr.setEncoding('utf8').

## 41. [P2] Unguarded readdirSync/statSync in the companion probes can crash the entire command at probe time

- **File:** `plugins/council/scripts/lib/discover.mjs:23` · **Category:** robustness-crash
- **Detail:** exists() is a try/catch existence check only — it does not verify the path is a directory, and it cannot cover the window between the check and the subsequent read. newestMatching() calls fs.readdirSync(dir) then fs.statSync(p) on the matched entries with no try/catch (discover.mjs:19-24), and findCodexCompanion() does the same over cacheRoot (discover.mjs:41-46). Every other filesystem access in this module is guarded (via exists()), so a throw here is uncaught and propagates out of probeBackends into the caller, aborting the run before any seat executes. This is fail-closed (no wrong result) but it turns a recoverable 'companion not found' into a hard crash. Fix: wrap the readdir/stat bodies in try/catch (return null / skip the entry on error), matching the fail-soft posture of the rest of the file.
- **Failure scenario:** Either (a) ~/.claude/plugins/cache/openai-codex/codex exists as a regular FILE rather than a directory → fs.readdirSync throws ENOTDIR; or (b) the Claude Code plugin auto-updater removes an old version subdir under ~/.claude/plugins/cache/agent-council/grok between newestMatching's readdirSync and its statSync → fs.statSync throws ENOENT (TOCTOU). The uncaught throw propagates through probeBackends and crashes the whole `audit`/`review`/`doctor` command with a stack trace instead of degrading to a 'companion not found' skip.
- **Verifier:** Confirmed real, low-severity fail-soft inconsistency. The FS calls are genuinely unguarded: discover.mjs:19,23 (newestMatching's fs.readdirSync + fs.statSync) and discover.mjs:41,45 (findCodexCompanion's fs.readdirSync + fs.statSync) have no try/catch. exists() (lines 8-14) wraps only fs.existsSync, which tests existence but NOT directory-ness or readability and follows symlinks — so the exists(dir) pre-check does not protect against readdir/stat throwing. probeBackends (line 130) and every calling handler (executeCouncilReview:600, handlePlan:2959, handleAudit, handleDoctor, etc.) also lack local try/catch, so a throw propagates to the single top-level catch in main() at council-companion.mjs:3621-3624, which prints error.message and sets process.exitCode=1 — aborting the whole review/audit/doctor run before any seat executes, instead of the intended companionAvailable:false skip. Concrete triggers: (a) EACCES when the cache dir exists but is unreadable by the process (Windows ACLs / managed profiles / dir created by another/elevated user) — existsSync passes, readdirSync throws; (b) ENOTDIR when a file or symlink-to-file sits at .../cache/openai-codex/codex or .../cache/agent-council/grok; (c) ENOENT TOCTOU when the plugin auto-updater removes a version subdir between readdir/exists and statSync (real but microsecond window). This is the only unguarded FS access in a module whose every other access routes through exists(), so a recoverable 'optional companion missing/inaccessible' escalates to a command abort. Correction to the claim: it IS caught at top level, so no raw stack trace is printed (only error.message + exit 1) — the 'crashes with a stack trace' wording is overstated, but the substantive wrong outcome (command aborts rather than degrading) holds. Uncommon triggers → P2, matching the claim.

## 42. [P2] collectGrokLimits off-by-one drops a first-line-only billing entry in sub-512KB logs

- **File:** `plugins/council/scripts/lib/token-usage.mjs:286` · **Category:** correctness
- **Detail:** The scan loop `for (let i = lines.length - 1; i > 0; i -= 1)` at line 286 never inspects `lines[0]`. The `i > 0` guard is meant to skip the tail's possibly-truncated first line, but when the log file is smaller than GROK_LOG_TAIL_BYTES (512KB) the read `start` is 0 and line 0 is a complete, valid record, not a truncated fragment. If the only line containing `creditUsagePercent` is at index 0 (and no later line matches), the loop finds nothing and the function returns null, so renderLimits reports Grok limits as 'unavailable: no billing entry ... (run the grok CLI once)' even though valid billing data is present. The guard should only skip index 0 when the tail was actually truncated (start > 0).
- **Failure scenario:** ~/.grok/logs/unified.jsonl is small (< 512KB), so the tail read starts at byte 0. Its first line is a complete `billing: fetched credits config` record with creditUsagePercent, and every subsequent line is some other message without that key. collectGrokLimits skips index 0 due to `i > 0`, matches nothing else, returns null, and the limits view falsely shows Grok as unavailable despite on-disk credit data.
- **Verifier:** Confirmed off-by-one. Line 273 sets start=Math.max(0,size-512KB), so any sub-512KB unified.jsonl is read whole (start===0) with no truncated first line. Yet the loop `for(let i=lines.length-1;i>0;i-=1)` at line 286 unconditionally skips lines[0]. The i>0 guard is meant to drop the possibly-truncated first fragment of a mid-file tail read (start>0), but it fires even when start===0 where lines[0] is a complete record. Concrete failure: a small log whose only creditUsagePercent record is the first line (e.g. a fresh single-run install where the billing fetch is the first logged line, or a one-line billing-only file where the loop body never executes since i=0 fails i>0) -> collectGrokLimits returns null -> renderLimits (line 330) prints 'unavailable: no billing entry ... (run the grok CLI once)' despite valid on-disk credit data. The existing test masks it because its fixture also has a newer billing entry at index 2. The bug is narrow (masked whenever any later billing line exists, which is the norm for multi-run logs), hence low severity, but it is a genuine reachable boundary defect. Correct fix: only skip index 0 when start>0.

## 43. [P2] metrics.jsonl append is an unlocked read-modify-write; concurrent job finishes silently drop entries

- **File:** `plugins/council/scripts/lib/metrics.mjs:91` · **Category:** concurrency-data-loss
- **Detail:** recordJobMetrics (line 91) and recordAuditMetrics (line 106) call appendJsonlCapped(metricsFile(cwd), ...), which in jsonl.mjs does readJsonl -> push -> writeJsonlCapped (a full atomic rewrite). There is no cross-process serialization around this read-modify-write. The sibling findings-ledger store performs the identical JSONL read-modify-write but wraps every mutation in withFileLock (ledger.mjs lines 68/157/187), and CHANGELOG line 77 documents that this lock was added specifically so that 'two council jobs finishing at once no longer lose each other's' entries. The metrics store was left without that lock. Each background council job runs in its own process (spawnBackgroundWorker) and records its metrics independently on completion, so the concurrent-finish scenario the ledger was hardened against is fully present here and unmitigated. Note: writeFileAtomic uses a per-PID tmp file + atomic rename, so the file is never left corrupt/torn — this is a lost-update (durability) bug, not corruption. Because metrics/ETA are best-effort telemetry that never change a command's exit code, the hard 'telemetry never changes outcome' invariant is not violated, hence P2 rather than P1; but the data loss is real and degrades both the `metrics` aggregation and the historical wall-clock that medianWallClockForKind uses to derive a running job's ETA.
- **Failure scenario:** Two background council jobs (e.g. from an endless/loop run or two concurrent `council review` jobs) finish within the few-ms IO window of the append. Process A reads N lines from metrics.jsonl; process B also reads the same N lines; A pushes its entry and atomically rewrites the file to N+1 lines; B pushes its own entry onto its stale N-line snapshot and atomically rewrites to N+1 lines, clobbering A's entry. Net result: A's metrics row is permanently lost, under-counting jobs/agent-calls and skewing the wall-clock history feeding the ETA estimate — exactly the lost-fingerprint failure the ledger's withFileLock was introduced to prevent, still unfixed for the metrics store.
- **Verifier:** Confirmed real. metrics.mjs recordJobMetrics (line 91) and recordAuditMetrics (line 106) call appendJsonlCapped, which in jsonl.mjs (lines 36-40) does readJsonl -> push -> writeJsonlCapped (full atomic rewrite) with NO cross-process lock. The sibling ledger store performs the identical JSONL read-modify-write but wraps every mutation in withFileLock (ledger.mjs 68/157/187), and CHANGELOG.md:77 documents that lock was added specifically so 'two council jobs finishing at once no longer lose each other's fingerprints'. The concurrent-finish path is fully present: spawnBackgroundWorker (council-companion.mjs:843) launches a detached, unref'd process per --background invocation (line 1009), and each worker calls recordJobMetrics independently on completion (677/717/787/837). Two such processes racing produce the classic lost-update (A reads N, B reads N, A writes N+1, B clobbers to its own N+1), permanently dropping a row. writeFileAtomic (per-PID tmp + rename) prevents corruption but not lost updates, so this is durability loss as claimed. Impact is real: the dropped row skews `metrics` aggregation (readMetrics, line 1590) and medianWallClockForKind's ETA history (lines 1636/1658). Could not refute — the guard the ledger received is demonstrably missing on the identical metrics write, and the motivating concurrent-worker path exists here.

## 44. [P2] stripComments blanks real import statements straddled by a string/regex-embedded /* ... */

- **File:** `plugins/council/scripts/lib/import-graph.mjs:22` · **Category:** correctness
- **Detail:** Line 22's block-comment regex `/\/\*[\s\S]*?\*\//g` runs over raw source with NO string/regex-literal awareness. A `/*` that occurs inside a string or regex literal pairs with the next `*/` (also possibly inside a literal) and every character between them — including real `import ... from` / `export ... from` statements — is replaced with spaces before parseModule ever sees it, so those edges silently disappear. This is an UNDOCUMENTED failure mode: the header comment lists only dynamic import(), computed specifiers and non-ESM consumers as invisible; it does not warn that a real static edge can be deleted. Because buildGraph feeds graph.importers, and audit-multifix.mjs derives both the consolidation plan (planConsolidation, line 39) and its fail-closed completeness gate (missingImporters, line 47; consulted at runOneTransform line 123) from graph.importers[victim], a dropped importer edge is invisible to the gate: the gate reports 'no missing importer', the consolidation auto-commits, and the un-rewritten importer is left referencing the emptied/moved victim -> broken import, caught only if that file happens to have covering char-tests. It also fabricates false 'orphan' and 'No test references' candidates. Verified latent (0 dropped edges across all 179 repo files today) but reproducible.
- **Failure scenario:** parseModule(`const OPEN = "/*";\nimport { x } from "./real.mjs";\nconst CLOSE = "*/";`) returns imports === [] (empirically confirmed). The edge to ./real.mjs is dropped: graph.importers["./real.mjs"] loses this importer, missingImporters() cannot see it, and a planConsolidation folding ./real.mjs's dependency can auto-commit while leaving this file's import unrewritten and broken.
- **Verifier:** CONFIRMED. Reproduced verbatim: parseModule(`const OPEN = "/*";\nimport { x } from "./real.mjs";\nconst CLOSE = "*/";`) returns imports:[] because stripComments (import-graph.mjs:22) runs /\/\*[\s\S]*?\*\//g over raw source with no string/regex-literal awareness — the `/*` inside the first string literal pairs with the `*/` inside the last, blanking the intervening real `import` statement to spaces before FROM_IMPORT sees it. This violates stripComments' own stated contract ("Remove block and line comments", line 19) by deleting real code, and is an UNDOCUMENTED failure mode: the header (lines 3-6) enumerates only edges that are *un-extractable* (dynamic import(), computed specifiers, non-ESM), not edge *deletion* of a statically-extractable import — so "documented limitation" does not refute it. Live consequence is real: buildGraph→findOrphanModules is wired (codebase-model.mjs:202/206, audit-logical.mjs:37), so a dropped A→V edge makes V a false orphan candidate. The multifix gate-defeat is also structurally real — planConsolidation (line 39) and its fail-closed missingImporters gate (line 47, consulted at runOneTransform:123) both read the same graph.importers[victim], so a missing edge is invisible to the very completeness gate meant to catch an un-rewritten importer. Kept at P2 (not higher) because it is fully latent: (a) 0 repo files trigger it today — this file's own line 22 uses escaped \/\* and \*\/ so no literal /* two-char sequence appears; (b) the auto-commit path (runMultiFix/planConsolidation) has NO live caller (grep: self-refs + tests only), matching the header's own "NOT wired into the loop yet"; (c) even when wired, the oracle (line 179-182) and full suite (line 183) backstop a broken import in any exercised file; and (d) the live orphan output is an explicitly low-confidence, never-auto-deleted review candidate. Real correctness bug, low current blast radius.

## 45. [P2] Lazy [\s\S]*? in FROM_IMPORT/EXPORT_FROM crosses statement boundaries and captures 'from "spec"' out of unrelated strings (phantom edge)

- **File:** `plugins/council/scripts/lib/import-graph.mjs:13` · **Category:** correctness
- **Detail:** FROM_IMPORT (line 10) and EXPORT_FROM (line 13) anchor at a statement boundary but their body `[\s\S]*?` is unbounded and freely crosses `;` and newlines into later statements. A bare local `export { ... }` (a re-export list with NO from) or a statement-leading `import.meta` therefore reaches forward to the next `from "..."` token anywhere in the file — including one embedded in an ordinary string literal — and captures it as an import specifier. If that string coincidentally names a real repo module, buildGraph adds a phantom edge. Effect on the graph reasoning: a phantom importer suppresses a legitimate orphan finding, can fabricate an import cycle, and can falsely mark a source module 'tested' (via testedSet in codebase-model.mjs). The stripComments pass does not help because the token is inside a string, not a comment.
- **Failure scenario:** parseModule(`export { foo };\nbar("see from './fake.mjs'");`) returns imports === ["./fake.mjs"], and parseModule(`import.meta.url;\nconsole.log("loaded from './fake.mjs'");`) also returns imports === ["./fake.mjs"] (both confirmed empirically). If ./fake.mjs exists in the file set, a phantom module->./fake.mjs edge is created, hiding ./fake.mjs from findOrphanModules and potentially inventing a cycle.
- **Verifier:** CONFIRMED. The lazy `[\s\S]*?` in FROM_IMPORT (line 10) and EXPORT_FROM (line 13) is unbounded and crosses `;`/newline statement boundaries. Empirically verified against the actual module: `export { foo };\nbar("see from './fake.mjs'")` → imports `["./fake.mjs"]`, and `import.meta.url;\nconsole.log("loaded from './fake.mjs'")` → `["./fake.mjs"]`. The trigger is in fact BROADER than the claim states: because `export const/function/class/{}` leads nearly every module, any statement-leading export/import token followed anywhere later by a string literal containing `from '<relative-path>'` captures it — e.g. `export const FOO = 1;\nthrow new Error("cannot import from './config.mjs'")` → `["./config.mjs"]`. Real re-exports (`export { B } from "./real.mjs"`) are still captured correctly, so this only ADDS phantom edges. Downstream harm is reachable and not a facade: buildGraph (lines 95-100) adds the edge with no legitimacy check whenever resolveImport finds the relative spec in the file set; codebase-model.mjs feeds this into findOrphanModules (a phantom edge puts the target in importedModules at line 197/201, silently suppressing a genuine orphan finding), into findCycles (a phantom back-edge can fabricate a Tarjan SCC), and into testedSet (a phantom edge from a *.test.mjs file marks an unrelated source module "tested", line 186). buildCodebaseModel is invoked from the live council-companion.mjs CLI. stripComments does not help because the token lives in a string literal, not a comment. Severity is capped at P2 (not higher) only because the module header explicitly declares the graph a low-confidence CANDIDATE signal, agent-verified, never used for auto-deletion — but the silent false-negative (hidden orphan) and fabricated cycle are genuine wrong outputs of a reached command, so it is a real correctness defect, not a style nit.

## 46. [P2] resumeContextKey joins fields without a delimiter, so distinct focus/policy inputs collide and a stale R1 is served on --resume

- **File:** `plugins/council/scripts/lib/resume.mjs:23` · **Category:** correctness
- **Detail:** resume.mjs lines 12-24: the prompt-shaping inputs are concatenated with `.join("")` (empty separator). Because there is no boundary marker between fields, two different option sets can hash to the same 12-char context key, and since the R1 cache is keyed by snapshotId+contextKey, a --resume run then reads the wrong seat's cached R1 and returns it with status:0/resumedFromCache:true as if fresh. This directly violates the module's stated invariant ('a different --focus ... does not reuse the wrong cached R1'). The most reachable collision is between the two adjacent free-text fields focusText and policyFocus (the caller r1ContextKey in deliberate.mjs:216-228 passes raw options.focusText and options.policyFocus into these two adjacent slots). Note the caller passes options.policyFocus (possibly undefined) into the key while the actual R1 prompt uses `policyFocus ?? focusText` (deliberate.mjs:257), so an undefined policyFocus keys as "" but prompts as focusText — which is exactly what enables the collision below. Fix: join with a separator that cannot appear in the values (e.g. a NUL/  or a length-prefixed encoding) so field boundaries are unambiguous.
- **Failure scenario:** On a fixed diff (same snapshotId), Run A uses focusText="a", policyFocus="b" -> key parts "a"+"b" = "ab..."; codex R1 (built for focus="a", policy="b") is cached. Run B (--resume) uses focusText="ab", policyFocus=undefined -> key parts "ab"+"" = "ab...", identical 12-char context key. readCachedR1 returns Run A's cached codex output, and it is merged as Run B's fresh codex R1 even though Run B's intended prompt (focus="ab", policy="ab") differs — a stale/mismatched cache served as fresh.
- **Verifier:** Confirmed and could not refute. resume.mjs:23 joins the prompt-shaping fields with `.join("")` (no delimiter), so two distinct option sets produce the LITERALLY IDENTICAL input string to sha256 — a deterministic structural collision, not a probabilistic hash collision. The two most-reachable colliding slots (index 0 focusText, index 1 policyFocus) are independent free-text inputs: focusText is the CLI positional (positionals.join(" ").trim()), policyFocus is policy.focus from the persisted policy config (loadPolicy). snapshotId (git-context.mjs:262) is diff-only, so contextKey is the sole discriminator for prompt inputs; if it collides, the cache slot collides. On --resume, deliberate.mjs:306 reads readCachedR1(snapshotId, seat, ctxKey) and a hit is merged directly as the seat's R1 with status:0/resumedFromCache:true (resume.mjs:73-87) — the prompt (policyFocus ?? focusText, deliberate.mjs:257) is never rebuilt for cached seats. Concrete failure (all other fields equal, same diff): Run A focus="a"+policy.focus="b" caches codex R1 under sha256("ab"+…); Run B --resume focus="ab"+no policy keys to the same string and is served Run A's codex output as fresh — a wrong-focus R1 merged as current. This directly violates the module's stated invariant (resume.mjs:8-11: "a different --focus ... does not reuse the wrong cached R1"). The cache read is wired into the live resume path (not a facade). P2 is right: real cache-poisoning correctness bug but resume-only/opt-in, needs a specific colliding input pairing, degrades review quality rather than data loss/security. Fix: join with a value-safe separator (NUL) or length-prefix each field.

## 47. [nit] Per-pass review never receives the reporter — B4/B5 per-unit/cell live updates are dead on the endless path

- **File:** `plugins/council/scripts/lib/audit-endless.mjs:131` · **Category:** facade
- **Detail:** runEndless holds the live reporter (options.reporter, line 95) and only fires it at PASS BOUNDARIES (reporter.phase/progress/budget/findings at lines 125-126, 144-145). Inside a pass it calls `review({ budget: passBudget, pass: passNo })` (line 131) — the review contract has NO reporter channel. The caller's closure (council-companion.mjs:2544-2556) spreads `...merged` and adds only budget/maxUnits/lensGroups/etc.; `merged` is CLI-options+policy and never contains a live reporter, so runAuditReview / runGroupedReview resolve `options.reporter ?? NOOP_REPORTER` to NOOP. Their finest-resolution telemetry — runGroupedReview's per-cell `reporter.progress({unitsDone})` + `reporter.findings(r.findings, {seat})` (audit-grouped-review.mjs:128-142, explicitly 'the FINEST resolution / fills fastest here') and runAuditReview's per-unit equivalents (audit-review.mjs:378-379) — writes to a NOOP and is never observed. The non-endless `audit review` path DOES thread the reporter (council-companion.mjs:2407), so the machinery exists and is reached everywhere except endless. This is the anti-facade class the invariants call out: built + wired + never reached on this path. NOTE on the clean fix (Grok's double-count warning is well-founded): `reporter.findings` in progress.mjs (lines 115-133, `cell.total += 1`) is purely additive with no dedupe. If you naively add `reporter` to the closure, each pass folds ALL of the pass's raw findings via audit-review.mjs:379 / audit-grouped-review.mjs:140, AND runEndless folds the deduped `fresh` again at line 145 — every finding is double-counted, and cross-pass duplicates that dedupeNew drops get re-counted every pass, so findingsByLens balloons. Correct fix: pass a wrapped reporter into the review closure whose `.findings()` is a no-op (delegating only phase/progress/seat/budget/eta), leaving runEndless the single owner of the deduped matrix via line 145; thread that muted reporter through review({budget, pass, reporter}).
- **Failure scenario:** Run `audit endless --groups lens` on a large repo where each pass dispatches ~maxCells grouped six-eyes cells over several minutes. runEndless calls review({budget,pass}) with no reporter; runGroupedReview gets NOOP_REPORTER. For the entire multi-minute pass the live dashboard's unitsDone bar and findings-by-lens table stay frozen at the pass-start snapshot — the per-cell progress the code advertises never moves. If someone 'fixes' it by adding reporter to the closure, the per-cell reporter.findings plus runEndless's reporter.findings(fresh) double-count every finding and re-count cross-pass duplicates each pass, inflating the matrix.
- **Verifier:** Mechanics verified: the `audit endless` review closure (council-companion.mjs:2544-2556) spreads `...merged` + budget/maxUnits/unitOffset/lensGroups/maxCells but never threads `reporter` (which isn't even created until line 2560). So runGroupedReview/runAuditReview on this path get NOOP_REPORTER, and their finest-resolution telemetry (grouped onCell reporter.progress/reporter.findings at audit-grouped-review.mjs:128/136-142; per-unit at audit-review.mjs:378-379) writes to NOOP. runEndless reports only at pass boundaries (audit-endless.mjs:125-126,144-145). The double-count note is also accurate (progress.mjs:129 cell.total+=1 is additive, no dedupe). HOWEVER the [P1]/anti-facade framing is unsupported: (1) this is pure telemetry and never changes the endless command's outcome/exit code/findings/convergence — the invariant explicitly classes the reporter as additive+fail-soft; (2) it is NOT a global facade — the per-cell reporter is reached and functional on the one-shot `audit review --groups` (2045-2054) and `audit fix --groups` (2395-2404) paths that thread a real reporter; on endless it's merely fed NOOP; (3) it matches documented design intent — the endless reporter comment (2558-2559) states reporting is 'per pass', and runEndless is the deliberate single owner of the deduped findings matrix (line 145), which is exactly why threading the raw reporter would double-count. Net: a real but minor, outcome-neutral telemetry-granularity gap (no intra-pass cell progress bar during a long endless pass), not a correctness/facade/data-loss defect. P1 is a gross overstatement; this is a nit.

