{
  "name": "agent-council-cc",
  "version": "2.1.0",
  "private": true,
  "description": "Claude Code plugin: multi-agent council code review (Claude + Codex + Grok)",
  "type": "module",
  "engines": {
    "node": ">=18.18"
  },
  "scripts": {
    "bump": "node scripts/bump-version.mjs",
    "lint": "node scripts/lint.mjs",
    "test:council-setup": "node plugins/council/scripts/council-companion.mjs setup --json",
    "test": "node --test"
  }
}
