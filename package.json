{
  "name": "agent-council-cc",
  "version": "0.2.0",
  "private": true,
  "description": "Claude Code plugins: Grok bridge + multi-agent council (Codex + Grok)",
  "type": "module",
  "engines": {
    "node": ">=18.18"
  },
  "scripts": {
    "test:grok-setup": "node plugins/grok/scripts/grok-companion.mjs setup --json",
    "test:council-setup": "node plugins/council/scripts/council-companion.mjs setup --json"
  }
}
